# 프로젝트 규칙

## 답변 원칙

### 기본 원칙
1. 항상 최신 데이터와 기존 데이터를 바탕으로 답변하며, 환각 현상(hallucination)을 방지하세요.
2. 모든 답변은 전문가 수준의 품질로 제공해야 합니다.
3. 약어를 사용할 때는 항상 전체 명칭(full form)을 먼저 기술한 뒤 약어를 함께 제공하세요.
   - 예: `Kubernetes (K8s)`, `Representational State Transfer (REST)`
   - 어떠한 경우에도 전체 명칭 없이 약어만 단독으로 사용해서는 안 됩니다.
4. 전문 용어, 개념, 변수명, 신호명 등이 처음 등장할 때 **무엇을 의미하는지 직관적으로 이해할 수 있는 최소한의 설명**을 함께 제공하세요.
   - 독자가 해당 분야를 처음 접하더라도 바로 이해할 수 있는 수준이어야 합니다.
   - 빠지는 부분 없이 전부 제공되어야 합니다. thoroughly
5. 설명이 장황하지 않고 핵심 인사이트가 직관적이게 제공되어야 하고 이해하기 쉬워야 합니다.
5. 전체 문단 흐름이 가독성이 좋아야 합니다.


### 기술사 시험 대비
1. 대한민국의 기술사(정보통신, 컴퓨터시스템응용) 시험을 준비중입니다.
2. 이 시험의 실제 답안지처럼 작성해야 합니다.
3. 제한시간이 있는 시험이므로 기술사 시험의 심사위원이 심사하고자 하는 핵심 요소를 놓쳐서는 안 됩니다.
4. **비교 관점 필수**: 개념 설명 시 유사 기술과의 비교표를 반드시 포함합니다.
   - 모든 파일에 "다른 것과 비교" 섹션 포함 (표 형태)
   - 예시 비교쌍: 스레드 vs 프로세스 / LRU vs FIFO vs OPT / TCP vs UDP / SSL vs IPSec / MSA vs 모놀리식 / 뮤텍스 vs 세마포어
5. **이해 다각화 필수**: 하나의 개념을 여러 관점으로 설명
   - 개념 정의 → 구조 다이어그램 → 동작 과정 → 비교표 → 실무 사례 → 어린이 비유
6. **기술사 8과목 체계** (pe_exam 폴더 구조 그대로):
   - 01 컴퓨터 구조, 02 운영체제, 03 데이터통신/네트워크
   - 04 소프트웨어 공학, 05 데이터베이스
   - 06 ICT 융합기술, 07 기업정보시스템, 08 알고리즘/통계

### 📋 과목별 핵심 주제 체크리스트

새 파일 작성 전, 하기 핵심 주제가 이미 있는지 반드시 확인하세요.

**01 컴퓨터 구조**: CPU 구조·파이프라인·hazard, 캐시메모리·지역성, 메모리 계층, RISC vs CISC, 인터럽트, DMA, RAID, 부동소수점, 병렬처리(SIMD·MIMD)
**02 운영체제**: 프로세스·스레드, CPU 스케줄링(FCFS/SJF/RR/MLFQ), 교착상태(4조건+해결), 동기화(세마포어·뮤텍스·모니터), IPC, 가상메모리·페이지교체(OPT/FIFO/LRU/Clock), 파일시스템
**03 네트워크**: OSI 7계층, TCP/UDP, IP주소·서브네팅·NAT, DNS·HTTP/HTTPS, 라우팅, 네트워크 장비, 무선통신·5G, VPN(SSL vs IPSec), 암호화·방화벽·IDS/IPS
**04 SW공학**: SDLC(폭포수·애자일·스크럼), 요구사항공학·유스케이스, UML(14종), 아키텍처패턴(MSA·계층형·이벤트드리븐), 디자인패턴(GoF 23종), 소프트웨어 테스트, 소프트웨어 품질(ISO 25010), CMMI, 형상관리, 프로젝트관리(WBS·CPM·EVM)
**05 데이터베이스**: 관계형 모델·SQL, 정규화(1~5NF·BCNF), 인덱스(B-Tree·해시), 트랜잭션·ACID, 동시성제어(Lock·MVCC·2PL), 회복기법, 분산DB·CAP, NoSQL(MongoDB·Redis·Cassandra), 데이터웨어하우스·OLAP·데이터마이닝
**06 ICT 융합**: AI/ML(지도·비지도·강화), 딥러닝(CNN·RNN·Transformer), 빅데이터(Hadoop·Spark), 클라우드(IaaS·PaaS·SaaS), 컨테이너·쿠버네티스, 블록체인, IoT, 보안(암호화·PKI·Zero Trust), 핀테크
**07 기업정보시스템**: ERP·SCM·CRM, SOA·ESB·EAI, BPM·BPR, IT전략(ISP·EA·BSC), IT거버넌스(COBIT·ITIL), BI·OLAP·데이터마이닝, DX, RPA, 컴플라이언스
**08 알고리즘/통계**: 정렬(퀵·합병·힙·기수), 탐색(이진·해시), 그래프(BFS·DFS·다익스트라·벨만포드·크루스칼·프림), DP(배낭·LCS·최장증가), 분할정복, 탐욕, 백트래킹, 복잡도(P·NP), 자료구조(힙·B트리·트라이), 통계(확률분포·가설검정·회귀)
**09 AI**

### 콘텐츠 품질 기준 (새 파일 작성 시)
- [ ] `categories = "pe_exam-과목명"` 형식 사용
- [ ] 핵심 인사이트 3줄 요약 포함
- [ ] ASCII 아트 다이어그램으로 구조 시각화
- [ ] 비교표 (대안 기술과의 차이) 포함
- [ ] 코드 예시 (Python 권장) 포함
- [ ] 실무에선? 섹션 (기술사 판단 기준) 포함
- [ ] 어린이를 위한 비유로 마무리

### 답변 구조

1. 무조건 길다고 좋은 것이 아니고 짧다고 좋은 것이 아닙니다.
2. **Ⅰ~Ⅴ 기술사 구조**가 기본 뼈대입니다. 각 섹션 안에 학습 콘텐츠(비유, 다이어그램, 코드)를 그대로 녹여 씁니다.
3. 아래 항목들은 **참고 요소**입니다. 주제에 따라 필요한 것만 골라 쓰고, 필요하면 새 항목을 추가해도 됩니다.
   - **개념**: 정의 및 기본 설명
   - **등장 배경**: 왜 필요하게 되었는가 (기존의 문제점, 한계, 필요성)
   - **구성 요소**: 주요 구성 요소 및 역할
   - **핵심 원리**: 동작 방식 (단계별 흐름, 알고리즘 등)
   - **장단점**: 비교 분석 (표 활용 권장)
   - **다른 것과 비교**: 대안 기술과의 비교 (선택 기준 포함)
   - **기술사적 판단**: 현업 관점 (실무 적용 사례, 선택 이유)
   - **미래 전망**: 발전 방향 및 트렌드
   - **활용 사례**: 실제 서비스/시스템에서의 사용 예시
   - **코드 예시**: 필요한 경우 핵심 코드 스니펫
   - **주의사항 / 흔한 실수**: 놓치기 쉬운 함정이나 안티패턴
   - **관련 개념**: 함께 알아야 할 연관 기술·용어
   - 매핑 참고: **개념·비유** → Ⅰ. 개요 / **구성 요소·다이어그램** → Ⅱ. / **장단점·비교표** → Ⅲ. / **실무·활용·주의사항** → Ⅳ. / **미래 전망** → Ⅴ.
4. 표나 다이어그램을 적극 활용해 주세요.
5. 추가적으로 `---` 선으로 분리하여 **어린이 버전 설명과 비유**를 들어 학습자가 쉽게 이해할 수 있도록 부록을 추가하세요.

## 파일 작성 규칙

1. **모든 새 글은 반드시 `content/` 안의 적절한 카테고리 폴더에 작성합니다.** `_posts/` 등 Jekyll 관례 경로에 글을 쓰지 마세요.
2. 파일명은 **소문자와 언더바(`_`)** 사용하세요.
   - 예: `heap_data_structure.md`, `docker_container.md`
3. 모든 파일은 반드시 아래 **Zola TOML front matter**로 시작해야 합니다:
   ```toml
   +++
   title = "제목"
   date = YYYY-MM-DD

   [extra]
   categories = "카테고리명"
   +++
   ```
   - `categories` 값은 아래 폴더 구조의 카테고리 중 하나 (예: `cs_fundamentals-network`, `programming-rust`)
4. 새 카테고리 폴더를 만들 때는 반드시 해당 디렉토리에 `_index.md` 파일도 함께 생성합니다:
   ```toml
   +++
   title = "섹션 제목"
   description = "설명"
   sort_by = "title"
   +++
   ```
5. 파일 본문은 한글로 작성합니다.
6. **동일한 제목의 파일이 이미 존재하는 경우, 새 파일을 따로 만들지 말고 기존 파일에 내용을 통합하세요.**
   - 중복되는 내용은 제거하고, 새로운 내용만 추가합니다.
   - 섹션 구조(헤더)가 겹치면 해당 섹션 안에 내용을 병합합니다.
   - 통합 후 전체 흐름과 가독성을 다시 한번 점검합니다.
7. **[필수] 모든 답변/파일 작성 완료 후 무조건 클립보드에 복사하세요. 절대 까먹지 마세요!**
   - macOS: `cat 파일경로 | pbcopy`
   - 새 파일 작성, 기존 파일 수정, 답변 완료 등 **모든 경우에 항상 pbcopy 실행**
   - 사용자에게 "클립보드에 복사 완료!"라고 안내


### ⚠️ 중요: 영역 혼용 금지
- **기술사 시험 내용**: 반드시 `pe_exam/` 하위에 작성
- **일반 학습/AI 심화**: `study/` 하위에 작성
- 두 영역을 절대 섞지 않습니다


## 📐 통합 답변 템플릿 (일반 + 기술사 융합)

> ⚠️ Ⅰ~Ⅴ 구조가 뼈대, 기존 12개 항목(개념·등장배경·구성요소·핵심원리·장단점·비교·판단·미래·활용·코드·주의사항·관련개념)이 살 — 두 구조를 완전히 녹인 단일 템플릿입니다.  
> 모든 항목이 필수는 아닙니다. 주제에 맞게 자유롭게 추가·생략·재구성하세요.

```markdown
# [주제명]

## 핵심 인사이트 (3줄 요약)
> 이게 뭔지, 왜 쓰는지, 어떻게 동작하는지를 딱 3줄로

---

### Ⅰ. 개요  ↔  개념 + 등장 배경
**개념**: [한 문장 정의]

> 비유: [쉬운 비유 한 줄]

**등장 배경**: 기존 [문제·한계] → [이 기술의 필요성]

---

### Ⅱ. 구성 요소 및 핵심 원리  ↔  구성 요소 + 핵심 원리 + 코드 예시
**구성 요소**:
| 구성 요소 | 역할 | 비유 |
|----------|------|------|
| ...      | ...  | ...  |

**핵심 원리** (ASCII 다이어그램 + 단계별 흐름):
```
[구조 다이어그램]
① 단계설명 → ② 단계설명 → ③ 단계설명
```

**코드 예시** ← 필요시 포함:
```언어
// 핵심 개념을 담은 최소 코드
```

---

### Ⅲ. 기술 비교 분석  ↔  장단점 + 다른 것과 비교
**장단점**:
| 장점 | 단점 |
|-----|------|
| ... | ...  |

**다른 것과 비교**:
| 항목 | [이것] | [대안 A] | [대안 B] |
|-----|--------|----------|----------|
| ... | ...    | ...      | ...      |

> **선택 기준**: [언제 이것을 쓰고, 언제 대안을 쓰는지 한 줄]

---

### Ⅳ. 실무 적용 방안  ↔  기술사적 판단 + 활용 사례 + 주의사항 / 흔한 실수 + 관련 개념
**기술사적 판단** (활용 사례 포함):
| 적용 분야 | 활용 방법 | 기대 효과 |
|---------|---------|------|
| ...     | ...     | ...  |

**주의사항 / 흔한 실수** ← 필요시:
- 놓치기 쉬운 함정
- 안티패턴

**관련 개념** ← 필요시: [함께 알아야 할 연관 기술·용어]

---

### Ⅴ. 기대 효과 및 결론  ↔  미래 전망
| 효과 영역 | 내용 | 정량적 목표 |
|---------|-----|-----------|
| ...     | ... | XX% 향상  |

> **결론**: [미래 전망 + 핵심 메시지 한두 줄]  
> **※ 참고**: [NIST, ISO, 과기정통부, IEEE 등 표준·가이드라인]

---

## 어린이를 위한 종합 설명
[전체 내용을 아주 쉬운 비유로 한 번 더 정리]

---
```

### 모의답안 작성 5대 원칙
1. **Ⅰ~Ⅴ 로마숫자 섹션**으로 구분 → 심사위원 빠른 채점 가능
2. **비교표 필수 포함** → 채점 기준 핵심 요소
3. **정량적 수치 활용** → "성능 XX% 향상", "비용 XX% 절감" 등
4. **최신 표준/가이드라인 인용** → NIST, ISO, 과기정통부, KISA 등
5. **학습 콘텐츠 통합** → 비유·코드·다이어그램을 Ⅱ·Ⅲ·Ⅳ 안에 자연스럽게 포함

### 콘텐츠 품질 체크리스트 (pe_exam/ 파일 기준)
- [ ] `categories = "pe_exam-과목명"` 형식 사용
- [ ] 핵심 인사이트 3줄 요약 포함
- [ ] Ⅰ. 개요: 개념 + 비유 + 등장 배경
- [ ] Ⅱ. 구성 요소: 표 + ASCII 다이어그램 + 단계별 프로세스
- [ ] Ⅲ. 비교: 장단점 표 + 대안 기술 비교표 + 선택 기준
- [ ] Ⅳ. 실무: 적용 시나리오 + 고려사항 (+ 코드 예시 필요시)
- [ ] Ⅴ. 결론: 정량 효과 표 + 미래 전망
- [ ] 어린이를 위한 종합 설명 (마지막 부록)

