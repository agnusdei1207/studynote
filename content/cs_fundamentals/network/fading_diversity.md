+++
title = "페이딩과 다이버시티 (Fading and Diversity)"
date = 2025-03-01

[extra]
categories = "cs_fundamentals-network"
+++

# 페이딩과 다이버시티 (Fading and Diversity)

## 핵심 인사이트 (3줄 요약)
> **페이딩**: 무선 신호가 경로, 장애물, 이동 등으로 약해지는 현상. **다이버시티**: 여러 독립 경로로 신호를 수신하여 신뢰성 향상. 무선 통신 품질의 핵심 요소이다.

## 1. 페이딩 (Fading)

### 1.1 개념
페이딩은 **무선 신호가 전파 과정에서 다양한 원인으로 약해지거나 왜곡되는 현상**이다.

> 비유: "라디오가 터널에서 끊김" - 신호가 약해지는 현상

### 1.2 페이딩 종류

```
대규모 페이딩 (Large Scale Fading):
- 거리에 따른 평균 신호 감쇠
- 경로 손실 (Path Loss)
- 그림자 페이딩 (Shadowing)

소규모 페이딩 (Small Scale Fading):
- 단거리 이동 시 급격한 변화
- 다중 경로 페이딩
- 시간/주파수 선택적 페이딩
```

### 1.3 다중 경로 페이딩
```
         직접파
      ┌──────────────→
      │
송신  │   반사파      수신
  ●───┼───────↗───────●
      │      /
      │  ↗  ↘
      │건물   ↘↘
                ↘↘↘
              산란파

원인:
- 반사 (Reflection)
- 굴절 (Refraction)
- 회절 (Diffraction)
- 산란 (Scattering)

결과:
- 위상 차이로 인한 보강/상쇄 간섭
```

### 1.4 페이딩 분류

```
시간 축:
┌────────────────────────────────────┐
│  시간 선택적 페이딩                 │
│  (Time Selective Fading)           │
│  - 도플러 효과                      │
│  - 채널이 시간에 따라 변화          │
│  - 빠른 이동 시 발생                │
└────────────────────────────────────┘

주파수 축:
┌────────────────────────────────────┐
│  주파수 선택적 페이딩               │
│  (Frequency Selective Fading)      │
│  - 특정 주파수 대역만 영향          │
│  - 다중 경로 지연 확산              │
│  - 심볼 간 간섭 (ISI) 발생          │
└────────────────────────────────────┘

평탄 페이딩:
┌────────────────────────────────────┐
│  전체 주파수 대역이 균일하게 영향   │
│  - 대역폭 < 코히어런스 대역폭       │
└────────────────────────────────────┘
```

### 1.5 레일리 vs 라이시안 페이딩

| 구분 | 레일리 (Rayleigh) | 라이시안 (Rician) |
|------|------------------|------------------|
| 조건 | 직접파 없음 | 직접파 존재 |
| 환경 | 도심, 실내 | 가시선 환경 |
| 분포 | 레일리 분포 | 라이시안 분포 |
| K-factor | 0 | > 0 |

## 2. 다이버시티 (Diversity)

### 2.1 개념
다이버시티는 **여러 개의 독립적인 경로로 신호를 수신**하여 하나의 경로가 페이딩되어도 다른 경로를 통해 신호를 복구하는 기술이다.

> 비유: "다리 건너기" - 여러 다리가 있으면 하나가 무너져도 건널 수 있음

### 2.2 다이버시티 종류

#### 공간 다이버시티 (Space Diversity)
```
┌─────┐
│ TX  │
└──┬──┘
   │
   ↓    경로1
   │ ────────────→ ┌───┐
   │               │RX1│
   │    경로2      └───┘
   │ ────────────→ ┌───┐
                  │RX2│
                  └───┘

특징:
- 여러 안테나 사용
- 간격: λ/2 이상
- 공간적 독립성 필요
```

#### 주파수 다이버시티 (Frequency Diversity)
```
주파수
  ↑
  │  f1 ──────→
  │
  │  f2 ──────→
  │
  │  f3 ──────→
  └───────────→ 시간

특징:
- 여러 주파수로 동시 전송
- OFDM에서 활용
- 주파수 간격 > 코히어런스 대역폭
```

#### 시간 다이버시티 (Time Diversity)
```
시간
  →
  ┌───┐         ┌───┐         ┌───┐
  │데이터│       │데이터│       │데이터│
  └───┘         └───┘         └───┘
    t1            t2            t3

특징:
- 여러 시간에 반복 전송
- 인터리빙 활용
- 간격 > 코히어런스 시간
```

#### 편파 다이버시티 (Polarization Diversity)
```
수직 편파: │
수평 편파: ─

특징:
- 서로 다른 편파 사용
- 공간 절약
- 3dB 손실 가능
```

### 2.3 합성 기법 (Combining)

```
1. 선택 합성 (Selection Combining, SC)
   - 가장 강한 신호 선택
   - 구현 단순
   - 성능: 낮음

2. 등이득 합성 (Equal Gain Combining, EGC)
   - 위상 정렬 후 합성
   - 가중치 동일
   - 성능: 중간

3. 최대비 합성 (Maximal Ratio Combining, MRC)
   - SNR에 비례하여 가중
   - 성능: 최적
   - 복잡도: 높음

MRC 수식:
y = Σ (h_i* × r_i) / Σ |h_i|²
```

## 3. 성능 비교

### 다이버시티 이득
```
BER vs SNR (N=2 다이버시티)

      │
BER   │    │무다이버시티
      │    │
      │     ╲
      │      ╲ N=2 다이버시티
      │       ╲
      └──────────────────→ SNR
              다이버시티 이득

다이버시티 이득:
- N개 안테나: ~10×log₁₀(N) dB
- 2안테나: ~3dB
- 4안테나: ~6dB
```

## 4. MIMO (Multiple Input Multiple Output)

```
다이버시티의 확장:

송신측                     수신측
┌───┐                     ┌───┐
│TX1│─────────────────────│RX1│
├───┤                     ├───┤
│TX2│─────────────────────│RX2│
└───┘                     └───┘

이점:
1. 다이버시티 게인
2. 공간 멀티플렉싱
3. 빔포밍

용량:
C = min(M, N) × B × log₂(1 + SNR)

M: 송신 안테나 수
N: 수신 안테나 수
B: 대역폭
```

## 5. 코드 예시

```python
import numpy as np
import random

class FadingChannel:
    """페이딩 채널 시뮬레이션"""

    def __init__(self, fading_type='rayleigh'):
        self.fading_type = fading_type

    def generate_rayleigh(self, num_samples):
        """레일리 페이딩 생성"""
        # 복소 가우시안 → 레일리
        h = (np.random.randn(num_samples) + 1j * np.random.randn(num_samples)) / np.sqrt(2)
        return h

    def generate_rician(self, num_samples, K_factor=3):
        """라이시안 페이딩 생성"""
        # LOS 성분 + NLOS 성분
        los = np.sqrt(K_factor / (K_factor + 1))
        nlos = np.sqrt(1 / (2 * (K_factor + 1)))
        h = los + nlos * (np.random.randn(num_samples) + 1j * np.random.randn(num_samples))
        return h

    def apply_fading(self, signal, fading_coeff):
        """페이딩 적용"""
        return signal * fading_coeff


class DiversityCombiner:
    """다이버시티 합성 시뮬레이션"""

    @staticmethod
    def selection_combining(signals, snrs):
        """선택 합성 (SC)"""
        # 최고 SNR 신호 선택
        best_idx = np.argmax(snrs)
        return signals[best_idx], best_idx

    @staticmethod
    def equal_gain_combining(signals):
        """등이득 합성 (EGC)"""
        # 위상 보정 후 합성
        phases = np.angle(signals)
        aligned = signals * np.exp(-1j * phases)
        combined = np.sum(aligned, axis=0) / np.sqrt(len(signals))
        return combined

    @staticmethod
    def maximal_ratio_combining(signals, channel_coeffs):
        """최대비 합성 (MRC)"""
        # 채널 이득에 비례한 가중치
        weights = np.conj(channel_coeffs)
        combined = np.sum(weights[:, np.newaxis] * signals, axis=0)
        combined /= np.sqrt(np.sum(np.abs(channel_coeffs)**2))
        return combined


class SpaceTimeDiversity:
    """공간-시간 다이버시티 시뮬레이션"""

    def __init__(self, num_tx=2, num_rx=1):
        self.num_tx = num_tx
        self.num_rx = num_rx

    def alamouti_encode(self, symbols):
        """Alamouti STBC 부호화 (2×1)"""
        encoded = []
        for i in range(0, len(symbols), 2):
            s1 = symbols[i]
            s2 = symbols[i+1] if i+1 < len(symbols) else 0

            # 시간 t: [s1, s2]
            # 시간 t+1: [-s2*, s1*]
            encoded.append([s1, s2])
            encoded.append([-np.conj(s2), np.conj(s1)])

        return np.array(encoded)

    def alamouti_decode(self, received, h1, h2):
        """Alamouti STBC 복호화"""
        r1 = received[0]  # 시간 t
        r2 = received[1]  # 시간 t+1

        # 복호화
        s1_est = (np.conj(h1) * r1 + h2 * np.conj(r2)) / (np.abs(h1)**2 + np.abs(h2)**2)
        s2_est = (np.conj(h2) * r1 - h1 * np.conj(r2)) / (np.abs(h1)**2 + np.abs(h2)**2)

        return s1_est, s2_est


# 시뮬레이션
print("=== 페이딩 채널 시뮬레이션 ===")
channel = FadingChannel()

rayleigh = channel.generate_rayleigh(1000)
rician = channel.generate_rician(1000, K_factor=3)

print(f"레일리 평균 전력: {np.mean(np.abs(rayleigh)**2):.4f}")
print(f"라이시안 평균 전력: {np.mean(np.abs(rician)**2):.4f}")

print("\n=== 다이버시티 합성 비교 ===")
# 2경로 다이버시티
num_samples = 1000
h1 = channel.generate_rayleigh(num_samples)
h2 = channel.generate_rayleigh(num_samples)

signal = np.ones(num_samples) + 1j * np.zeros(num_samples)
noise_power = 0.1

r1 = h1 * signal + np.sqrt(noise_power/2) * (np.random.randn(num_samples) + 1j * np.random.randn(num_samples))
r2 = h2 * signal + np.sqrt(noise_power/2) * (np.random.randn(num_samples) + 1j * np.random.randn(num_samples))

snr1 = np.abs(h1)**2 / noise_power
snr2 = np.abs(h2)**2 / noise_power

# 선택 합성
sc_result, best_idx = DiversityCombiner.selection_combining([r1, r2], [np.mean(snr1), np.mean(snr2)])
print(f"선택 합성 SNR: {max(np.mean(snr1), np.mean(snr2)):.2f} dB")

# 최대비 합성
mrc_result = DiversityCombiner.maximal_ratio_combining(np.array([r1, r2]), np.array([h1, h2]))
mrc_snr = (np.mean(np.abs(h1)**2) + np.mean(np.abs(h2)**2)) / noise_power
print(f"최대비 합성 SNR: {10*np.log10(mrc_snr):.2f} dB")

print("\n=== Alamouti STBC ===")
stbc = SpaceTimeDiversity(num_tx=2, num_rx=1)
symbols = [1+1j, 1-1j, -1+1j, -1-1j]
encoded = stbc.alamouti_encode(symbols)
print(f"부호화된 심볼 수: {len(encoded)}")
print(f"첫 두 시간 슬롯: {encoded[0]}, {encoded[1]}")
```

## 6. 장단점

### 다이버시티
| 장점 | 단점 |
|-----|------|
| 신뢰성 향상 | 하드웨어 증가 |
| 간단한 개념 | 전력 소모 증가 |
| 다양한 구현 | 공간 필요 |

### 합성 기법
| 기법 | 장점 | 단점 |
|------|------|------|
| SC | 단순 | 성능 낮음 |
| EGC | 중간 성능 | 위상 추적 |
| MRC | 최적 성능 | 복잡 |

## 7. 실무에선? (기술사적 판단)
- **WiFi**: 공간 다이버시티 (2×2 MIMO)
- **LTE/5G**: 대규모 MIMO
- **위성**: 주파수 다이버시티
- **차량 통신**: 시간 다이버시티

## 8. 관련 개념
- MIMO
- 빔포밍
- 채널 추정
- 등화

---

## 어린이를 위한 종합 설명

**페이딩은 "라디오 끊김"이에요!**

### 왜 끊기나요? 📻
```
전파가 여러 길로 와요:

  ────→ 건물에 부딪힘!
      ↗
     /
●──→ ────→ 터널에서 약해짐
     \
      ↘
  ────→ 산에 부딪힘!

신호가 엉켜서 약해져요!
```

### 다이버시티로 해결! 🎯
```
여러 길로 보내요:

길1: ─────→
길2: ─────→
길3: ─────→

하나가 막혀도 다른 길로 와요!
```

### 합성 기법 🎵
```
선택: 제일 큰 소리만 듣기
등이득: 모두 같게 섞기
최대비: 큰 소리는 더 크게!
```

**비밀**: 스마트폰 안테나가 여러 개인 이유가 이거예요! 📱✨
