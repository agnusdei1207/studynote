# 병렬 처리 (Parallel Processing)

## 핵심 인사이트 (3줄 요약)
> **여러 작업을 동시에 처리**하여 성능을 향상시키는 기술. 데이터 병렬성과 작업 병렬성으로 나뉜다. 멀티코어, GPU, 슈퍼컴퓨터의 핵심 개념이다.

## 1. 개념
병렬 처리(Parallel Processing)는 **하나의 문제를 여러 부분으로 나누어 동시에 처리**하는 컴퓨팅 기법이다.

> 비유: "여러 사람이 짐을 나눠 듦" - 혼자보다 빠름

## 2. 병렬성 종류

### 데이터 병렬성
```
같은 연산을 여러 데이터에 적용

예: 배열의 모든 요소에 +1

[1, 2, 3, 4]
   ↓ 병렬 처리
[2, 3, 4, 5]

→ SIMD, GPU
```

### 작업 병렬성
```
다른 연산을 동시에 수행

예: 웹 서버
- 스레드 1: 이미지 처리
- 스레드 2: DB 쿼리
- 스레드 3: 로그 기록

→ MIMD, 멀티코어
```

## 3. 병렬 처리 수준

```
1. 명령어 수준 (ILP)
   - 파이프라이닝
   - 슈퍼스칼라

2. 스레드 수준 (TLP)
   - 멀티스레딩
   - 하이퍼스레딩

3. 코어 수준 (CMP)
   - 멀티코어

4. 시스템 수준
   - 클러스터
   - 분산 시스템
```

## 4. 아므달의 법칙 (Amdahl's Law)

```
병렬화 가능한 비율 P, 프로세서 수 N

Speedup = 1 / ((1-P) + P/N)

예: 80% 병렬화, 4코어
Speedup = 1 / (0.2 + 0.8/4) = 1 / 0.4 = 2.5배

교훈: 순차 부분(1-P)이 병목!
```

## 5. 병렬 처리 모델

| 모델 | 설명 | 예시 |
|------|------|------|
| SIMD | 단일 명령, 다중 데이터 | GPU |
| MIMD | 다중 명령, 다중 데이터 | 멀티코어 |
| SPMD | 단일 프로그램, 다중 데이터 | MPI |

## 6. 장단점

| 장점 | 단점 |
|-----|------|
| 성능 향상 | 프로그래밍 복잡 |
| 처리량 증가 | 동기화 오버헤드 |
| 확장성 | 디버깅 어려움 |

## 7. 코드 예시

```c
// OpenMP 병렬 처리
#include <omp.h>

int main() {
    int sum = 0;

    #pragma omp parallel for reduction(+:sum)
    for (int i = 0; i < 1000; i++) {
        sum += i;
    }

    printf("Sum: %d\n", sum);
    return 0;
}
```

## 8. 실무에선? (기술사적 판단)
- **AI/ML**: GPU 병렬 처리 필수
- **빅데이터**: Spark, Hadoop
- **웹 서버**: 멀티스레드/멀티프로세스
- **게임**: 멀티스레드 렌더링

## 9. 관련 개념
- 아므달의 법칙
- SIMD/MIMD
- 동시성 (Concurrency)

---

## 어린이를 위한 종합 설명

**병렬 처리는 "분업"이야!**

### 예전 방식 (순차)
```
숙제: 100문제 풀기

나 혼자: 1번 → 2번 → 3번 → ...
         10시간 걸려요! 😰
```

### 병렬 처리
```
친구 10명이서:

친구1: 1~10번
친구2: 11~20번
...
친구10: 91~100번

→ 1시간이면 끝! 🎉
```

### 아므달의 법칙
```
하지만... 처음에 문제 설명은 30분 걸려요
→ 아무리 친구 많아도 30분은 줄일 수 없어요!

이게 병목이에요! 🐌
```

**비밀**: AI, 게임, 영상 처리... 모든 게 병렬 처리 덕분에 빨라요! 🚀
