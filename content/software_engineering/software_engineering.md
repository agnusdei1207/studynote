+++
title = "소프트웨어 공학 (Software Engineering)"
date = 2025-03-01

[extra]
categories = "software_engineering-methodology"
+++

# 소프트웨어 공학 (Software Engineering)

## 핵심 인사이트 (3줄 요약)
> **소프트웨어 개발의 체계적인 접근 방법**을 연구하는 학문. 품질, 비용, 일정의 균형을 맞춰 신뢰할 수 있는 소프트웨어를 개발하는 것이 목표. 방법론, 도구, 프로세스를 포함한다.

## 1. 개념
소프트웨어 공학은 **소프트웨어의 개발, 운영, 유지보수에 체계적인 공학적 원칙을 적용**하는 학문이다.

> 비유: "건축 공학" - 집을 짓는 데 체계적인 방법과 원칙을 적용하듯, 소프트웨어도 마찬가지

## 2. 등장 배경: 소프트웨어 위기

```
1960년대 후반~1970년대 소프트웨어 위기:

문제점:
1. 개발 비용 증가
   - 하드웨어 비용 ↓
   - 소프트웨어 비용 ↑

2. 개발 기간 지연
   - 일정 초과 빈번

3. 품질 저하
   - 신뢰성 부족
   - 오류 다수

4. 유지보수 어려움
   - 문서 부족
   - 구조 파악 곤란

5. 사용자 요구 불만족
   - 요구사항 반영 실패

1968년 NATO 회의에서 "Software Engineering" 용어 처음 사용
```

## 3. 소프트웨어 특성

```
1. 무형성 (Intangibility)
   - 눈에 보이지 않음
   - 물리적 속성 없음

2. 복잡성 (Complexity)
   - 하드웨어보다 복잡
   - 상태 조합 무한

3. 일치성 (Conformity)
   - 물리 법칙 따르지 않음
   - 인위적 제약

4. 변경성 (Changeability)
   - 끊임없는 수정
   - 버그 수정, 기능 추가

5. 비가시성 (Invisibility)
   - 구조 시각화 어려움
```

## 4. 소프트웨어 공학의 3요소

```
┌─────────────────────────────────────────┐
│          소프트웨어 공학 3요소           │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────┐    ┌─────────┐            │
│  │ 방법론   │    │  도구   │            │
│  │(Method) │    │ (Tool)  │            │
│  └────┬────┘    └────┬────┘            │
│       │              │                  │
│       └──────┬───────┘                  │
│              │                          │
│              ▼                          │
│        ┌─────────┐                      │
│        │프로세스 │                      │
│        │(Process)│                      │
│        └─────────┘                      │
│                                         │
└─────────────────────────────────────────┘

방법론 (Methodology):
- 어떻게 개발할 것인가
- 예: 폭포수, 애자일, 객체지향

도구 (Tool):
- 무엇을 사용할 것인가
- 예: IDE, 버전관리, 이슈트래커

프로세스 (Process):
- 어떤 순서로 할 것인가
- 예: 요구분석 → 설계 → 구현 → 테스트
```

## 5. 소프트웨어 개발 생명주기 (SDLC)

```
SDLC (Software Development Life Cycle):

┌────────────────────────────────────────────────────┐
│                                                    │
│  ┌─────────┐                                      │
│  │  계획   │ 프로젝트 범위, 일정, 비용 수립        │
│  └────┬────┘                                      │
│       │                                           │
│       ▼                                           │
│  ┌─────────┐                                      │
│  │요구분석 │ 사용자 요구사항 수집, 분석           │
│  └────┬────┘                                      │
│       │                                           │
│       ▼                                           │
│  ┌─────────┐                                      │
│  │  설계   │ 시스템 구조, 상세 설계               │
│  └────┬────┘                                      │
│       │                                           │
│       ▼                                           │
│  ┌─────────┐                                      │
│  │  구현   │ 코딩, 단위 테스트                    │
│  └────┬────┘                                      │
│       │                                           │
│       ▼                                           │
│  ┌─────────┐                                      │
│  │  테스트 │ 통합, 시스템, 인수 테스트            │
│  └────┬────┘                                      │
│       │                                           │
│       ▼                                           │
│  ┌─────────┐                                      │
│  │운영/유지│ 배포, 유지보수                       │
│  │보수     │                                      │
│  └─────────┘                                      │
│                                                    │
└────────────────────────────────────────────────────┘
```

## 6. 소프트웨어 공학 원칙

```
1. 추상화 (Abstraction)
   - 불필요한 세부사항 숨김
   - 핵심 개념에 집중

2. 정보 은닉 (Information Hiding)
   - 모듈 내부 구조 숨김
   - 인터페이스만 공개

3. 모듈화 (Modularity)
   - 기능별 분리
   - 독립적 개발, 테스트

4. 단계적 분해 (Stepwise Refinement)
   - 상위에서 하위로
   - 점진적 상세화

5. 국소화 (Localization)
   - 관련 요소 모으기
   - 응집도 높이기

6. 균일성 (Uniformity)
   - 일관된 표기법, 스타일
   - 코딩 표준 준수

7. 완전성 (Completeness)
   - 모든 요구사항 충족
   - 누락 없음
```

## 7. 코드 예시

```python
from dataclasses import dataclass
from typing import List, Dict, Optional
from enum import Enum
import json

class SDLCPhase(Enum):
    """SDLC 단계"""
    PLANNING = "계획"
    ANALYSIS = "요구분석"
    DESIGN = "설계"
    IMPLEMENTATION = "구현"
    TESTING = "테스트"
    DEPLOYMENT = "배포"
    MAINTENANCE = "유지보수"

class PhaseStatus(Enum):
    """단계 상태"""
    NOT_STARTED = "미시작"
    IN_PROGRESS = "진행중"
    COMPLETED = "완료"
    BLOCKED = "차단됨"

@dataclass
class Requirement:
    """요구사항"""
    id: str
    description: str
    priority: str  # 높음, 중간, 낮음
    status: str
    assigned_to: Optional[str] = None

@dataclass
class Milestone:
    """마일스톤"""
    name: str
    phase: SDLCPhase
    due_date: str
    status: PhaseStatus

class SoftwareProject:
    """소프트웨어 프로젝트 관리"""

    def __init__(self, name: str):
        self.name = name
        self.requirements: List[Requirement] = []
        self.milestones: List[Milestone] = []
        self.current_phase = SDLCPhase.PLANNING
        self.team: List[str] = []
        self.documents: Dict[str, str] = {}

    def add_requirement(self, req: Requirement):
        """요구사항 추가"""
        self.requirements.append(req)

    def add_milestone(self, milestone: Milestone):
        """마일스톤 추가"""
        self.milestones.append(milestone)

    def add_team_member(self, name: str):
        """팀원 추가"""
        self.team.append(name)

    def create_document(self, doc_type: str, content: str):
        """문서 생성"""
        self.documents[doc_type] = content

    def get_progress(self) -> Dict:
        """진행 상황 조회"""
        total_reqs = len(self.requirements)
        completed_reqs = len([r for r in self.requirements if r.status == "완료"])

        return {
            "current_phase": self.current_phase.value,
            "total_requirements": total_reqs,
            "completed_requirements": completed_reqs,
            "progress_percent": (completed_reqs / total_reqs * 100) if total_reqs > 0 else 0,
            "total_milestones": len(self.milestones),
            "completed_milestones": len([m for m in self.milestones if m.status == PhaseStatus.COMPLETED])
        }

    def advance_phase(self):
        """다음 단계로 진행"""
        phases = list(SDLCPhase)
        current_idx = phases.index(self.current_phase)
        if current_idx < len(phases) - 1:
            self.current_phase = phases[current_idx + 1]
            return True
        return False


class QualityMetrics:
    """품질 지표"""

    def __init__(self):
        self.defects_found = 0
        self.defects_fixed = 0
        self.test_cases = 0
        self.test_cases_passed = 0
        self.code_reviews = 0

    @property
    def defect_density(self) -> float:
        """결함 밀도 (결함/KLOC)"""
        # 실제로는 코드 라인 수로 나눔
        return self.defects_found

    @property
    def fix_rate(self) -> float:
        """결함 수정률"""
        if self.defects_found == 0:
            return 0
        return (self.defects_fixed / self.defects_found) * 100

    @property
    def test_pass_rate(self) -> float:
        """테스트 통과율"""
        if self.test_cases == 0:
            return 0
        return (self.test_cases_passed / self.test_cases) * 100


# 사용 예시
print("=== 소프트웨어 프로젝트 시뮬레이션 ===\n")

# 프로젝트 생성
project = SoftwareProject("전자상거래 시스템")

# 팀 구성
project.add_team_member("김개발")
project.add_team_member("이설계")
project.add_team_member("박테스트")

# 요구사항 추가
project.add_requirement(Requirement("REQ-001", "사용자 로그인 기능", "높음", "완료"))
project.add_requirement(Requirement("REQ-002", "상품 검색 기능", "높음", "진행중"))
project.add_requirement(Requirement("REQ-003", "장바구니 기능", "중간", "미시작"))
project.add_requirement(Requirement("REQ-004", "결제 기능", "높음", "미시작"))

# 마일스톤 설정
project.add_milestone(Milestone("M1", SDLCPhase.ANALYSIS, "2024-01-15", PhaseStatus.COMPLETED))
project.add_milestone(Milestone("M2", SDLCPhase.DESIGN, "2024-02-01", PhaseStatus.COMPLETED))
project.add_milestone(Milestone("M3", SDLCPhase.IMPLEMENTATION, "2024-03-01", PhaseStatus.IN_PROGRESS))

# 문서 생성
project.create_document("요구사항 명세서", "사용자 기능 요구사항...")
project.create_document("설계 문서", "시스템 아키텍처...")

# 진행 상황
print("프로젝트:", project.name)
print("현재 단계:", project.current_phase.value)
print("\n팀원:", ", ".join(project.team))

progress = project.get_progress()
print(f"\n진행률: {progress['progress_percent']:.1f}%")
print(f"요구사항: {progress['completed_requirements']}/{progress['total_requirements']} 완료")
print(f"마일스톤: {progress['completed_milestones']}/{progress['total_milestones']} 완료")

# 품질 지표
print("\n=== 품질 지표 ===")
quality = QualityMetrics()
quality.defects_found = 25
quality.defects_fixed = 20
quality.test_cases = 100
quality.test_cases_passed = 92

print(f"결함 수정률: {quality.fix_rate:.1f}%")
print(f"테스트 통과율: {quality.test_pass_rate:.1f}%")
```

## 8. 소프트웨어 공학 지식 체계

```
SWEBOK (Software Engineering Body of Knowledge):

1. 요구사항 공학
2. 소프트웨어 설계
3. 소프트웨어 구축
4. 소프트웨어 테스트
5. 소프트웨어 유지보수
6. 소프트웨어 형상관리
7. 소프트웨어 공학 관리
8. 소프트웨어 공학 프로세스
9. 소프트웨어 공학 도구 및 방법
10. 소프트웨어 품질
11. 관련 학문 (수학, 컴퓨팅, 공학)
```

## 9. 장단점

### 체계적 개발의 장점
| 장점 | 설명 |
|-----|------|
| 품질 향상 | 체계적인 프로세스 |
| 예측 가능성 | 일정, 비용 추정 |
| 유지보수성 | 문서화, 구조화 |
| 협업 용이 | 역할, 책임 명확 |

### 과도한 문서화의 단점
| 단점 | 설명 |
|-----|------|
| 시간 소요 | 문서 작성 부담 |
| 유연성 저하 | 변경 어려움 |
| 형식주의 | 내용보다 형식 |

## 10. 실무에선? (기술사적 판단)
- **프로젝트 규모**: 소규모는 애자일, 대규모는 체계적 프로세스
- **안전 중요**: 의료, 항공 등은 엄격한 프로세스
- **하이브리드**: 워터폴 + 애자일 혼합
- **DevOps**: 개발과 운영의 통합
- **지속적 개선**: 프로세스 지속적 개선

## 11. 관련 개념
- SDLC
- 애자일
- DevOps
- 형상관리
- 품질관리

---

## 어린이를 위한 종합 설명

**소프트웨어 공학은 "레고 조립 설명서" 같아요!**

### 왜 필요할까? 🏗️
```
무작정 만들면:
- 엉망이 될 수 있어요 😵
- 고치기 어려워요
- 다른 사람이 못 봐요

계획적으로 만들면:
- 튼튼해요 💪
- 고치기 쉬워요
- 다른 사람도 이해해요
```

### 3가지 요소 🛠️
```
방법론: 어떻게 만들까?
  "레고 조립 순서"

도구: 뭘로 만들까?
  "조립 도구"

프로세스: 어떤 순서로?
  "1단계 → 2단계 → 3단계"
```

### 개발 단계 📋
```
1. 계획: 뭘 만들까?
2. 요구분석: 어떤 기능이 필요해?
3. 설계: 어떻게 생겼으면 좋겠어?
4. 구현: 실제로 만들기!
5. 테스트: 잘 작동하나?
6. 배포: 사용자에게 전달
7. 유지보수: 계속 고치고 개선
```

**비밀**: 좋은 소프트웨어는 좋은 공학으로 만들어져요! 🎯✨
