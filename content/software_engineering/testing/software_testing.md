+++
title = "소프트웨어 테스트 (Software Testing)"
date = 2025-03-01

[extra]
categories = "software_engineering-testing"
+++

# 소프트웨어 테스트 (Software Testing)

## 핵심 인사이트 (3줄 요약)
> **소프트웨어의 결함을 발견하고 품질을 검증**하는 활동. 단위→통합→시스템→인수 테스트로 진행. 블랙박스(명세 기반)와 화이트박스(구조 기반)로 분류.

## 1. 개념
소프트웨어 테스트는 **소프트웨어가 요구사항을 만족하는지 확인하고 결함을 발견**하기 위한 활동이다.

> 비유: "자동차 안전 검사" - 출고 전에 모든 기능을 테스트

## 2. 테스트의 목적

```
1. 결함 발견 (Defect Detection)
   - 버그 찾기
   - 품질 문제 식별

2. 품질 검증 (Quality Verification)
   - 요구사항 충족 확인
   - 기능적/비기능적 요구 검증

3. 신뢰성 확보 (Reliability Assurance)
   - 오류 없는 실행 보장
   - 사용자 신뢰 구축

4. 정보 제공 (Information Providing)
   - 상태 파악
   - 의사결정 지원
```

## 3. 테스트 레벨 (V-모델)

```
          요구사항 분석 ──────────────── 인수 테스트
                   ↓                          ↑
               설계 ────────────────── 시스템 테스트
                   ↓                          ↑
            상세 설계 ──────────────── 통합 테스트
                   ↓                          ↑
               구현 ────────────────── 단위 테스트
                   ↓                          ↑
                   └──────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                   테스트 레벨                           │
├────────────────────────────────────────────────────────┤
│                                                        │
│  1. 단위 테스트 (Unit Test)                            │
│     - 대상: 모듈, 함수, 클래스                         │
│     - 수행자: 개발자                                   │
│     - 방법: 화이트박스                                 │
│                                                        │
│  2. 통합 테스트 (Integration Test)                     │
│     - 대상: 모듈 간 인터페이스                         │
│     - 수행자: 개발자/테스터                            │
│     - 방법: 화이트박스 + 블랙박스                      │
│                                                        │
│  3. 시스템 테스트 (System Test)                        │
│     - 대상: 전체 시스템                                │
│     - 수행자: 테스터                                   │
│     - 방법: 블랙박스                                   │
│                                                        │
│  4. 인수 테스트 (Acceptance Test)                      │
│     - 대상: 사용자 요구사항                            │
│     - 수행자: 사용자/고객                              │
│     - 방법: 블랙박스                                   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

## 4. 테스트 기법 분류

### 4.1 테스트 레벨별
```
┌────────────────────────────────────────────────────────┐
│                     테스트 기법                         │
├────────────────────────────────────────────────────────┤
│                                                        │
│  정적 테스트 (Static Testing)                          │
│  - 코드 실행 없이 검사                                 │
│  - 코드 리뷰, 정적 분석                                │
│                                                        │
│  동적 테스트 (Dynamic Testing)                         │
│  - 코드 실행하며 검사                                  │
│  - 블랙박스, 화이트박스                                │
│                                                        │
└────────────────────────────────────────────────────────┘
```

### 4.2 블랙박스 vs 화이트박스
```
┌────────────────┬─────────────────┬─────────────────┐
│      항목       │   블랙박스      │   화이트박스    │
├────────────────┼─────────────────┼─────────────────┤
│ 내부 구조      │ 모름            │ 앎              │
│ 기반          │ 명세, 요구사항  │ 코드, 구조      │
│ 관점          │ 사용자          │ 개발자          │
│ 기법          │ 동등분할, 경계값│ 구문, 조건 커버 │
│ 언제          │ 시스템, 인수    │ 단위, 통합      │
└────────────────┴─────────────────┴─────────────────┘
```

## 5. 블랙박스 테스트 기법

### 5.1 동등 분할 (Equivalence Partitioning)
```
입력을 동등한 그룹으로 나누어 대표값만 테스트

예: 나이 입력 (0-150)
┌───────────┬───────────┬───────────┐
│  유효하지  │   유효함   │  유효하지  │
│   않음     │  0-150    │   않음     │
│   < 0     │           │   > 150   │
└───────────┴───────────┴───────────┘
     -1            25           200

테스트 케이스:
- -1 (무효)
- 25 (유효)
- 200 (무효)
```

### 5.2 경계값 분석 (Boundary Value Analysis)
```
경계값에서 오류 발생 확률이 높음

예: 1-100 점수 입력
┌────────────────────────────────────────┐
│ 0 │ 1 │ 2 │ ... │ 99 │ 100 │ 101 │    │
└───┴───┴───┴─────┴────┴─────┴─────┘
  ↑   ↑           ↑    ↑     ↑
  최소-1 최소   최대-1 최대  최대+1

테스트 케이스:
- 0 (경계 미만)
- 1 (경계)
- 100 (경계)
- 101 (경계 초과)
```

### 5.3 원인-결과 그래프 (Cause-Effect Graph)
```
입력 조건(원인)과 출력(결과)의 관계 분석

예: 로그인
원인:
C1: ID 올바름
C2: PW 올바름

결과:
E1: 로그인 성공
E2: 에러 메시지

      C1 ──┐
           ├── AND ── E1
      C2 ──┘

      NOT C1 ──┐
               ├── OR ── E2
      NOT C2 ──┘
```

## 6. 화이트박스 테스트 기법

### 6.1 검증 기준
```
┌────────────────────────────────────────────────────────┐
│                  화이트박스 검증 기준                   │
├────────────────────────────────────────────────────────┤
│                                                        │
│  1. 구문 커버리지 (Statement Coverage)                 │
│     - 모든 문장이 최소 1회 실행                        │
│     - 기준: 100%                                       │
│                                                        │
│  2. 분기 커버리지 (Branch Coverage)                    │
│     - 모든 분기(T/F)가 최소 1회 실행                   │
│     - 기준: 100%                                       │
│                                                        │
│  3. 조건 커버리지 (Condition Coverage)                 │
│     - 모든 조건이 T/F가 최소 1회 실행                  │
│                                                        │
│  4. 분기/조건 커버리지                                 │
│     - 분기 + 조건 모두 만족                            │
│                                                        │
│  5. 변형 조건/분기 커버리지 (MC/DC)                    │
│     - 각 조건이 독립적으로 결과에 영향                 │
│                                                        │
│  6. 다중 조건 커버리지                                 │
│     - 모든 조건 조합 테스트                            │
│                                                        │
│  7. 경로 커버리지 (Path Coverage)                      │
│     - 모든 실행 경로 테스트                            │
│                                                        │
└────────────────────────────────────────────────────────┘
```

### 6.2 기본 경로 테스트
```
순환 복잡도(Cyclomatic Complexity) 기반

V(G) = E - N + 2
E: 엣지 수, N: 노드 수

또는
V(G) = P + 1
P: 조건문 수

예:
     ┌─────┐
     │ 시작│
     └──┬──┘
        │
     ┌──▼──┐
     │ if  │ ← 분기
     └──┬──┘
    ┌───┴───┐
    ▼       ▼
   ┌──┐   ┌──┐
   │T │   │F │
   └──┘   └──┘
    │       │
    └───┬───┘
        │
     ┌──▼──┐
     │ 끝  │
     └─────┘

V(G) = 2 (분기 1개)
→ 최소 2개 테스트 케이스 필요
```

## 7. 테스트 종류

```
1. 회귀 테스트 (Regression Test)
   - 수정 후 기존 기능 영향 확인
   - 자동화 필수

2. 스모크 테스트 (Smoke Test)
   - 주요 기능 빠르게 확인
   - 빌드 후 즉시 수행

3. 알파/베타 테스트
   - 알파: 개발 환경, 내부 사용자
   - 베타: 실제 환경, 외부 사용자

4. 성능 테스트
   - 부하 테스트
   - 스트레스 테스트
   - 병목 테스트

5. 보안 테스트
   - 취약점 스캔
   - 침투 테스트

6. 사용성 테스트
   - UI/UX 평가
   - A/B 테스트
```

## 8. 코드 예시

```python
from dataclasses import dataclass
from typing import List, Callable, Any
from enum import Enum
import unittest

# ===== 테스트 대상 코드 =====
class Calculator:
    """간단한 계산기"""

    def add(self, a: int, b: int) -> int:
        return a + b

    def divide(self, a: int, b: int) -> float:
        if b == 0:
            raise ValueError("0으로 나눌 수 없습니다")
        return a / b

    def is_valid_age(self, age: int) -> bool:
        """0-150 사이의 나이인지 확인"""
        return 0 <= age <= 150

    def get_grade(self, score: int) -> str:
        """점수를 학점으로 변환 (1-100)"""
        if score < 0 or score > 100:
            raise ValueError("점수는 0-100 사이여야 합니다")
        if score >= 90:
            return "A"
        elif score >= 80:
            return "B"
        elif score >= 70:
            return "C"
        elif score >= 60:
            return "D"
        else:
            return "F"


# ===== 블랙박스 테스트 =====
class BlackBoxTest(unittest.TestCase):
    """블랙박스 테스트 예시"""

    def setUp(self):
        self.calc = Calculator()

    # 동등 분할
    def test_age_equivalence_partitioning(self):
        """동등 분할 테스트"""
        # 무효 영역 (음수)
        self.assertFalse(self.calc.is_valid_age(-1))

        # 유효 영역
        self.assertTrue(self.calc.is_valid_age(25))

        # 무효 영역 (151 이상)
        self.assertFalse(self.calc.is_valid_age(151))

    # 경계값 분석
    def test_score_boundary_value(self):
        """경계값 분석 테스트"""
        # 경계 미만
        with self.assertRaises(ValueError):
            self.calc.get_grade(-1)

        # 경계 (최소)
        self.assertEqual(self.calc.get_grade(0), "F")

        # 경계 (최대)
        self.assertEqual(self.calc.get_grade(100), "A")

        # 경계 초과
        with self.assertRaises(ValueError):
            self.calc.get_grade(101)

        # 등급 경계
        self.assertEqual(self.calc.get_grade(89), "B")
        self.assertEqual(self.calc.get_grade(90), "A")


# ===== 화이트박스 테스트 =====
class WhiteBoxTest(unittest.TestCase):
    """화이트박스 테스트 예시"""

    def setUp(self):
        self.calc = Calculator()

    # 구문 커버리지
    def test_statement_coverage(self):
        """모든 문장 실행"""
        # add 함수의 모든 문장 실행
        result = self.calc.add(1, 2)
        self.assertEqual(result, 3)

    # 분기 커버리지
    def test_branch_coverage(self):
        """모든 분기 실행"""
        # True 분기
        self.assertTrue(self.calc.is_valid_age(25))

        # False 분기 (음수)
        self.assertFalse(self.calc.is_valid_age(-1))

        # False 분기 (초과)
        self.assertFalse(self.calc.is_valid_age(200))

    # 조건 커버리지
    def test_condition_coverage(self):
        """조건의 T/F 조합"""
        # is_valid_age: (age >= 0) and (age <= 150)
        # 조건1 T, 조건2 T → True
        self.assertTrue(self.calc.is_valid_age(25))

        # 조건1 F, 조건2 T → False
        self.assertFalse(self.calc.is_valid_age(-1))

        # 조건1 T, 조건2 F → False
        self.assertFalse(self.calc.is_valid_age(200))

    # 경로 커버리지
    def test_path_coverage(self):
        """모든 경로 실행"""
        # get_grade의 모든 경로 테스트
        self.assertEqual(self.calc.get_grade(95), "A")  # path 1
        self.assertEqual(self.calc.get_grade(85), "B")  # path 2
        self.assertEqual(self.calc.get_grade(75), "C")  # path 3
        self.assertEqual(self.calc.get_grade(65), "D")  # path 4
        self.assertEqual(self.calc.get_grade(55), "F")  # path 5


# ===== 테스트 케이스 관리 =====
@dataclass
class TestCase:
    """테스트 케이스"""
    id: str
    description: str
    precondition: str
    input_data: Any
    expected_result: Any
    actual_result: Any = None
    passed: bool = None

class TestManager:
    """테스트 관리자"""

    def __init__(self):
        self.test_cases: List[TestCase] = []
        self.results = {"passed": 0, "failed": 0, "total": 0}

    def add_test_case(self, tc: TestCase):
        """테스트 케이스 추가"""
        self.test_cases.append(tc)

    def execute_test(self, tc: TestCase, test_func: Callable):
        """테스트 실행"""
        try:
            tc.actual_result = test_func(tc.input_data)
            tc.passed = tc.actual_result == tc.expected_result
        except Exception as e:
            tc.actual_result = str(e)
            tc.passed = False

        if tc.passed:
            self.results["passed"] += 1
        else:
            self.results["failed"] += 1
        self.results["total"] += 1

        return tc.passed

    def generate_report(self) -> str:
        """테스트 보고서 생성"""
        report = []
        report.append("=" * 50)
        report.append("테스트 보고서")
        report.append("=" * 50)
        report.append(f"총 테스트: {self.results['total']}")
        report.append(f"통과: {self.results['passed']}")
        report.append(f"실패: {self.results['failed']}")
        report.append(f"통과율: {self.results['passed']/self.results['total']*100:.1f}%")
        report.append("=" * 50)

        report.append("\n상세 결과:")
        for tc in self.test_cases:
            status = "✓ 통과" if tc.passed else "✗ 실패"
            report.append(f"{tc.id}: {tc.description} - {status}")

        return "\n".join(report)


# 사용 예시
print("=== 소프트웨어 테스트 시뮬레이션 ===\n")

# 테스트 실행
print("--- 단위 테스트 실행 ---")
suite = unittest.TestSuite()
suite.addTests(unittest.TestLoader().loadTestsFromTestCase(BlackBoxTest))
suite.addTests(unittest.TestLoader().loadTestsFromTestCase(WhiteBoxTest))

runner = unittest.TextTestRunner(verbosity=2)
runner.run(suite)

# 테스트 관리자
print("\n--- 테스트 관리자 ---")
manager = TestManager()
calc = Calculator()

# 테스트 케이스 추가
manager.add_test_case(TestCase(
    "TC-001", "덧셈 테스트", "없음",
    (1, 2), 3
))

manager.add_test_case(TestCase(
    "TC-002", "나눗셈 테스트", "없음",
    (10, 2), 5.0
))

manager.add_test_case(TestCase(
    "TC-003", "0으로 나누기", "없음",
    (10, 0), "0으로 나눌 수 없습니다"
))

# 테스트 실행
for tc in manager.test_cases:
    if tc.id == "TC-001":
        manager.execute_test(tc, lambda x: calc.add(*x))
    elif tc.id == "TC-002":
        manager.execute_test(tc, lambda x: calc.divide(*x))
    elif tc.id == "TC-003":
        try:
            calc.divide(*tc.input_data)
        except ValueError as e:
            tc.actual_result = str(e)
            tc.passed = tc.actual_result == tc.expected_result
            if tc.passed:
                manager.results["passed"] += 1
            else:
                manager.results["failed"] += 1
            manager.results["total"] += 1

# 보고서 출력
print(manager.generate_report())
```

## 9. 테스트 원칙

```
1. 살충제 패러독스 (Pesticide Paradox)
   - 같은 테스트 반복하면 새 버그 못 찾음
   - 테스트 케이스 지속적 갱신 필요

2. 테스트는 결함이 있음을 보여줌
   - 결함이 없음을 증명할 수 없음

3. 완전 테스트 불가능
   - 모든 경로, 입력 조합 불가능
   - 위험 기반으로 우선순위 결정

4. 테스트 일찍 시작
   - 비용 적게 듦
   - 조기 결함 발견

5. 결함 집중 (Defect Clustering)
   - 80% 결함이 20% 모듈에 집중
   - 파레토 법칙
```

## 10. 장단점

### 테스트의 장점
| 장점 | 설명 |
|-----|------|
| 품질 향상 | 결함 조기 발견 |
| 비용 절감 | 나중에 고치는 비용 큼 |
| 신뢰성 | 사용자 신뢰 |
| 문서화 | 테스트가 명세 역할 |

### 테스트의 한계
| 한계 | 설명 |
|-----|------|
| 완전 불가 | 모든 경우 테스트 불가 |
| 시간 소요 | 충분한 테스트에 시간 필요 |
| 인력 필요 | 테스트 전문가 필요 |
| 자동화 비용 | 초기 투자 필요 |

## 11. 실무에선? (기술사적 판단)
- **TDD**: 테스트 주도 개발로 품질 향상
- **자동화**: CI/CD 파이프라인 통합
- **커버리지**: 70-80% 목표, 100%는 비효율
- **위험 기반**: 중요 기능 우선 테스트
- **도구**: JUnit, pytest, Selenium, JMeter

## 12. 관련 개념
- V-모델
- TDD
- 테스트 자동화
- 품질 보증

---

## 어린이를 위한 종합 설명

**소프트웨어 테스트는 "시험" 같아요!**

### 왜 필요할까요? 📝
```
테스트 없이 출시하면:
- 버그가 많아요 🐛
- 사용자가 화나요 😠
- 나중에 고치기 비싸요 💰

테스트하고 출시하면:
- 버그를 미리 찾아요 🔍
- 사용자가 좋아요 😊
- 미리 고쳐요!
```

### 테스트 종류 🧪
```
단위 테스트: 작은 부분 하나하나
통합 테스트: 연결 부분
시스템 테스트: 전체 시스템
인수 테스트: 사용자가 OK?
```

### 테스트 방법 🔬
```
블랙박스: 속을 모르고 테스트
  "버튼 누르면 켜지나?"

화이트박스: 속을 알고 테스트
  "이 코드가 실행되나?"
```

### 경계값 테스트 🎯
```
0-100 점수:
- 0점 (최소) ✓
- 100점 (최대) ✓
- -1점 (안 됨) ✗
- 101점 (안 됨) ✗

경계에서 실수가 많아요!
```

**비밀**: 좋은 개발자는 테스트를 좋아해요! 🧪✨
