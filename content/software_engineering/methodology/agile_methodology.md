+++
title = "애자일 방법론 (Agile Methodology)"
date = 2025-03-01

[extra]
categories = "software_engineering-methodology"
+++

# 애자일 방법론 (Agile Methodology)

## 핵심 인사이트 (3줄 요약)
> **변화에 유연하게 대응하는 반복적 소프트웨어 개발 방식**. 짧은 주기로 개발하고 피드백을 반영. 협업, 고객 중심, 작동하는 소프트웨어를 중시한다.

## 1. 개념
애자일(Agile)은 **2001년 애자일 선언문을 통해 등장한 유연한 소프트웨어 개발 방법론**으로, 변화를 수용하고 고객과 협업하며 작동하는 소프트웨어를 빠르게 전달하는 것을 목표로 한다.

> 비유: "요리의 맛보기" - 완성 전에 계속 맛을 보며 조리

## 2. 애자일 선언문 (Agile Manifesto)

```
2001년, 17명의 소프트웨어 개발자가 모여 선언

┌─────────────────────────────────────────────────────────┐
│                애자일 소프트웨어 개발 선언               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  우리는 소프트웨어를 개발하면서 더 나은 방법을 찾고     │
│  다른 사람들을 도와주면서 다음 가치를 더 중요하게       │
│  여깁니다:                                              │
│                                                         │
│  공정과 도구보다     [개인과 상호작용]을                 │
│  포괄적인 문서보다   [작동하는 소프트웨어]을             │
│  계약 협상보다       [고객과의 협력]을                   │
│  계획을 따르기보다   [변화에 대응]을                     │
│                                                         │
│  즉, 오른쪽의 가치도 중요하지만                         │
│  우리는 왼쪽의 가치에 더 높은 가치를 둡니다.            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 3. 애자일 12원칙

```
1. 고객 만족
   - 가치 있는 소프트웨어를 빠르고 지속적으로 전달

2. 변화 수용
   - 개발 후반라도 요구사항 변경을 환영

3. 짧은 릴리즈
   - 몇 주~몇 개월 단위로 작동하는 소프트웨어 전달

4. 협업
   - 비즈니스 담당자와 개발자는 매일 함께 일함

5. 동기 부여된 팀
   - 의지 있는 개인 중심, 환경과 지원 제공

6. 대면 커뮤니케이션
   - 효율적인 정보 전달 방법

7. 작동하는 소프트웨어
   - 진척도의 1차 척도

8. 지속 가능한 속도
   - 일정한 속도로 무기한 개발 가능

9. 기술적 우수성
   - 좋은 설계와 기술적 탁월성에 주목

10. 단순화
    - 안 하는 일의 양을 최대화

11. 자기 조직화 팀
    - 스스로 최선의 아키텍처, 요구사항, 설계 도출

12. 회고
    - 정기적으로 효율성을 반성하고 개선
```

## 4. 애자일 vs 전통적 방법론

```
┌────────────────┬─────────────────┬─────────────────┐
│      항목       │    전통적 방법   │     애자일      │
├────────────────┼─────────────────┼─────────────────┤
│ 접근 방식      │ 순차적          │ 반복적, 점진적  │
│ 요구사항      │ 초기에 고정     │ 지속적 변경     │
│ 문서화        │ 상세한 문서     │ 최소 문서       │
│ 고객 참여     │ 시작/끝        │ 지속적          │
│ 변경 비용     │ 높음            │ 낮음            │
│ 위험 노출     │ 후반            │ 초기            │
│ 팀 구성       │ 전문 분야별     │ 교차 기능 팀    │
│ 성공 기준     │ 계획 준수       │ 고객 가치       │
└────────────────┴─────────────────┴─────────────────┘
```

## 5. 주요 애자일 방법론

### 5.1 스크럼 (Scrum)
```
특징:
- 2-4주 스프린트
- 명확한 역할 (PO, SM, 팀)
- 정기 회의 (데일리, 리뷰, 회고)

구조:
┌─────────────────────────────────────┐
│            Product Backlog          │
│        (제품 백로그: 할 일 목록)    │
└────────────────┬────────────────────┘
                 │ 스프린트 계획
                 ▼
┌─────────────────────────────────────┐
│           Sprint Backlog            │
│      (스프린트 백로그: 이번 할 일)  │
└────────────────┬────────────────────┘
                 │ 2-4주 개발
                 ▼
┌─────────────────────────────────────┐
│           Potentially Shippable     │
│              Increment              │
│       (배포 가능한 제품 증분)       │
└─────────────────────────────────────┘
```

### 5.2 XP (Extreme Programming)
```
특징:
- 짧은 주기 (1-2주)
- 테스트 주도 개발 (TDD)
- 페어 프로그래밍
- 지속적 통합 (CI)

핵심 실천법:
┌─────────────────────────────────────┐
│ 1. Pair Programming (페어 프로그래밍)│
│ 2. TDD (테스트 주도 개발)           │
│ 3. CI (지속적 통합)                 │
│ 4. Simple Design (단순한 설계)      │
│ 5. Refactoring (리팩토링)           │
│ 6. Collective Ownership (공유 소유) │
│ 7. On-site Customer (현장 고객)     │
│ 8. Sustainable Pace (지속 가능 속도)│
└─────────────────────────────────────┘
```

### 5.3 칸반 (Kanban)
```
특징:
- 지속적 흐름
- WIP(Work In Progress) 제한
- 시각화 보드

칸반 보드:
┌───────────┬───────────┬───────────┬───────────┐
│   할 일   │   진행중   │   검토중   │   완료    │
│   (To Do) │ (Doing)   │ (Review)  │  (Done)   │
├───────────┼───────────┼───────────┼───────────┤
│   [WIP 3] │  [WIP 2]  │  [WIP 2]  │           │
├───────────┼───────────┼───────────┼───────────┤
│ □ 기능1   │ ■ 기능4   │ ■ 기능6   │ ✓ 기능8   │
│ □ 기능2   │ ■ 기능5   │ ■ 기능7   │ ✓ 기능9   │
│ □ 기능3   │           │           │ ✓ 기능10  │
└───────────┴───────────┴───────────┴───────────┘

WIP 제한:
- 동시 진행 작업 수 제한
- 병목 현상 방지
```

## 6. 애자일 프로세스

```
┌─────────────────────────────────────────────────────────┐
│                    애자일 반복 주기                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ┌──────────┐                                         │
│   │ 계획 수립 │                                         │
│   └────┬─────┘                                         │
│        │                                               │
│        ▼                                               │
│   ┌──────────┐      ┌──────────┐                      │
│   │   설계   │ ───→ │  구현    │                      │
│   └──────────┘      └────┬─────┘                      │
│                          │                            │
│                          ▼                            │
│                    ┌──────────┐                       │
│                    │  테스트  │                       │
│                    └────┬─────┘                       │
│                         │                             │
│                         ▼                             │
│                   ┌──────────┐                        │
│                   │  리뷰    │                        │
│                   └────┬─────┘                        │
│                        │                              │
│                        ▼                              │
│                   ┌──────────┐                        │
│                   │  회고    │ ──→ 다음 반복          │
│                   └──────────┘                        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 7. 코드 예시

```python
from dataclasses import dataclass, field
from typing import List, Optional
from enum import Enum
from datetime import datetime, timedelta

class Priority(Enum):
    """우선순위"""
    HIGH = "높음"
    MEDIUM = "중간"
    LOW = "낮음"

class TaskStatus(Enum):
    """작업 상태"""
    TODO = "할 일"
    IN_PROGRESS = "진행중"
    REVIEW = "검토중"
    DONE = "완료"

@dataclass
class UserStory:
    """사용자 스토리"""
    id: str
    title: str
    description: str  # ~로서, ~를 하고 싶다. 그래서 ~한다.
    priority: Priority
    story_points: int  # 난이도/크기
    status: TaskStatus = TaskStatus.TODO
    tasks: List['Task'] = field(default_factory=list)

@dataclass
class Task:
    """세부 작업"""
    id: str
    title: str
    assignee: str
    status: TaskStatus = TaskStatus.TODO
    hours: int = 0

@dataclass
class Sprint:
    """스프린트"""
    number: int
    start_date: datetime
    end_date: datetime
    goal: str
    stories: List[UserStory] = field(default_factory=list)

    @property
    def velocity(self) -> int:
        """벨로시티 (완료된 스토리 포인트)"""
        return sum(s.story_points for s in self.stories if s.status == TaskStatus.DONE)

class AgileBoard:
    """애자일 칸반 보드"""

    def __init__(self, wip_limits: dict = None):
        self.backlog: List[UserStory] = []
        self.sprints: List[Sprint] = []
        self.current_sprint: Optional[Sprint] = None
        self.wip_limits = wip_limits or {
            TaskStatus.IN_PROGRESS: 3,
            TaskStatus.REVIEW: 2
        }

    def add_story(self, story: UserStory):
        """스토리 추가"""
        self.backlog.append(story)

    def create_sprint(self, weeks: int = 2, goal: str = "") -> Sprint:
        """스프린트 생성"""
        start = datetime.now()
        end = start + timedelta(weeks=weeks)

        sprint = Sprint(
            number=len(self.sprints) + 1,
            start_date=start,
            end_date=end,
            goal=goal
        )
        self.sprints.append(sprint)
        self.current_sprint = sprint
        return sprint

    def add_to_sprint(self, story: UserStory):
        """스프린트에 스토리 추가"""
        if self.current_sprint and story in self.backlog:
            self.current_sprint.stories.append(story)
            self.backlog.remove(story)

    def move_story(self, story: UserStory, new_status: TaskStatus):
        """스토리 상태 변경"""
        # WIP 제한 확인
        if self.current_sprint:
            in_status = len([s for s in self.current_sprint.stories
                           if s.status == new_status])
            if new_status in self.wip_limits and in_status >= self.wip_limits[new_status]:
                print(f"WIP 제한 도달: {new_status.value}")
                return False

        story.status = new_status
        return True

    def get_burndown_data(self) -> List[tuple]:
        """번다운 차트 데이터"""
        # 실제로는 일별 데이터 수집
        total_points = sum(s.story_points for s in self.current_sprint.stories)
        return [(0, total_points), (self.current_sprint.end_date, 0)]

    def daily_standup(self):
        """데일리 스탠드업 미팅"""
        print("\n=== 데일리 스탠드업 ===")
        print("어제 한 일:")
        for story in self.current_sprint.stories:
            if story.status == TaskStatus.IN_PROGRESS:
                for task in story.tasks:
                    if task.status == TaskStatus.IN_PROGRESS:
                        print(f"  - {task.assignee}: {task.title}")

        print("\n오늘 할 일:")
        for story in self.current_sprint.stories:
            if story.status == TaskStatus.TODO:
                print(f"  - {story.title}")

        print("\n장애 요소:")
        # 팀원들이 직접 말하는 자리

    def sprint_retrospective(self):
        """스프린트 회고"""
        print("\n=== 스프린트 회고 ===")
        print(f"스프린트 #{self.current_sprint.number}")
        print(f"목표: {self.current_sprint.goal}")
        print(f"벨로시티: {self.current_sprint.velocity}")

        print("\n잘한 점 (Keep):")
        print("개선할 점 (Problem):")
        print("시도할 점 (Try):")


# 사용 예시
print("=== 애자일 프로젝트 시뮬레이션 ===\n")

# 칸반 보드 생성
board = AgileBoard()

# 사용자 스토리 추가
stories = [
    UserStory("US-001", "로그인", "사용자로서 로그인을 하고 싶다", Priority.HIGH, 5),
    UserStory("US-002", "검색", "사용자로서 상품을 검색하고 싶다", Priority.HIGH, 8),
    UserStory("US-003", "장바구니", "사용자로서 상품을 담고 싶다", Priority.MEDIUM, 5),
    UserStory("US-004", "결제", "사용자로서 결제하고 싶다", Priority.HIGH, 13),
    UserStory("US-005", "리뷰", "사용자로서 리뷰를 쓰고 싶다", Priority.LOW, 3),
]

for story in stories:
    board.add_story(story)

# 스프린트 생성
sprint = board.create_sprint(weeks=2, goal="핵심 기능 구현")
print(f"스프린트 #{sprint.number} 시작: {sprint.goal}")
print(f"기간: {sprint.start_date.date()} ~ {sprint.end_date.date()}")

# 스프린트에 스토리 추가
board.add_to_sprint(stories[0])  # 로그인
board.add_to_sprint(stories[1])  # 검색
board.add_to_sprint(stories[3])  # 결제

print(f"\n스프린트 백로그: {len(sprint.stories)}개 스토리")
print(f"총 스토리 포인트: {sum(s.story_points for s in sprint.stories)}")

# 작업 진행
board.move_story(stories[0], TaskStatus.IN_PROGRESS)
board.move_story(stories[1], TaskStatus.IN_PROGRESS)

# 데일리 스탠드업
board.daily_standup()

# 작업 완료
board.move_story(stories[0], TaskStatus.DONE)

# 회고
board.sprint_retrospective()
```

## 8. 장단점

### 장점
| 장점 | 설명 |
|-----|------|
| 유연성 | 변경에 빠르게 대응 |
| 고객 만족 | 지속적 피드백 반영 |
| 품질 향상 | 빈번한 테스트, 리팩토링 |
| 위험 감소 | 초기에 문제 발견 |
| 협업 강화 | 팀 간 긴밀한 소통 |

### 단점
| 단점 | 설명 |
|-----|------|
| 범위 관리 어려움 | 계속 추가되는 요구사항 |
| 문서 부족 | 유지보수 시 어려움 |
| 팀 역량 의존 | 숙련된 팀 필요 |
| 일정 예측 어려움 | 정확한 완료일 예측 곤란 |
| 대규모 프로젝트 | 적용 어려움 |

## 9. 실무에선? (기술사적 판단)
- **스타트업**: 애자일 필수, 빠른 시장 적응
- **웹/모바일**: 2주 스프린트, 지속적 배포
- **SI/SM**: 하이브리드 (계약은 워터폴, 개발은 애자일)
- **대규모**: SAFe, LeSS 등 스케일드 애자일
- **도구**: Jira, Trello, Azure DevOps

## 10. 관련 개념
- 스크럼 (Scrum)
- XP (Extreme Programming)
- 칸반 (Kanban)
- 스프린트
- 벨로시티

---

## 어린이를 위한 종합 설명

**애자일은 "요리의 맛보기" 같아요!**

### 전통적 방식 vs 애자일 🍳
```
전통적 방식:
1. 요리 계획 완벽히 세우기 📋
2. 재료 모두 손질하기
3. 요리 완성까지 기다리기
4. 먹어보니 짜요! 😭
5. 다시 만들기 어려워요

애자일:
1. 간단히 계획하기 📝
2. 조금 만들어 맛보기 😋
3. "조금 더 짜게 해주세요"
4. 바로 수정하기 ✏️
5. 계속 개선해요!
```

### 4가치 💎
```
사람이 중요해요! (도구보다)
작동하는 게 중요해요! (문서보다)
함께 일해요! (계약보다)
변화를 환영해요! (계획보다)
```

### 스크럼 🏃
```
2주마다:
1. 뭘 할까요? (계획)
2. 매일 만나요 (데일리)
3. 완성해요 (리뷰)
4. 잘한 점, 못한 점 (회고)
```

**비밀**: 넷플릭스, 스포티파이도 애자일로 만들어요! 🎵✨
