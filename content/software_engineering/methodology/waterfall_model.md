+++
title = "폭포수 모델 (Waterfall Model)"
date = 2025-03-01

[extra]
categories = "software_engineering-methodology"
+++

# 폭포수 모델 (Waterfall Model)

## 핵심 인사이트 (3줄 요약)
> **순차적으로 진행하는 전통적 소프트웨어 개발 모델**. 요구분석→설계→구현→테스트→유지보수 순서. 단계가 끝나면 다음 단계로만 진행하며 되돌아가기 어렵다.

## 1. 개념
폭포수 모델은 **1970년 Winston Royce가 제안한 순차적 소프트웨어 개발 방법론**으로, 각 단계가 폭포처럼 아래로만 흐르는 특징이 있다.

> 비유: "폭포" - 위에서 아래로만 떨어지고 거꾸로 올라갈 수 없음

## 2. 폭포수 모델 단계

```
        ┌─────────────────┐
        │   요구사항 분석   │ ← 사용자 요구 수집, 명세화
        └────────┬────────┘
                 │ ↓
        ┌─────────────────┐
        │      설계       │ ← 시스템 구조, 상세 설계
        └────────┬────────┘
                 │ ↓
        ┌─────────────────┐
        │      구현       │ ← 코딩, 단위 테스트
        └────────┬────────┘
                 │ ↓
        ┌─────────────────┐
        │     테스트      │ ← 통합, 시스템, 인수 테스트
        └────────┬────────┘
                 │ ↓
        ┌─────────────────┐
        │    운영/유지보수  │ ← 배포, 유지보수
        └─────────────────┘

각 단계의 결과물:
요구분석 → 요구사항 명세서 (SRS)
설계 → 설계 문서 (SDD)
구현 → 소스 코드
테스트 → 테스트 보고서
운영 → 사용자 매뉴얼
```

## 3. 각 단계 상세

```
1. 요구사항 분석 (Requirements Analysis)
   ┌─────────────────────────────────────┐
   │ - 사용자 요구 수집                  │
   │ - 요구사항 분석, 명세화             │
   │ - 요구사항 검토                     │
   │ - 결과물: 요구사항 명세서 (SRS)     │
   └─────────────────────────────────────┘

2. 설계 (Design)
   ┌─────────────────────────────────────┐
   │ - 시스템 아키텍처 설계              │
   │ - 상세 설계 (모듈, DB, UI)          │
   │ - 인터페이스 설계                   │
   │ - 결과물: 설계 문서 (SDD)           │
   └─────────────────────────────────────┘

3. 구현 (Implementation)
   ┌─────────────────────────────────────┐
   │ - 소스 코드 작성                    │
   │ - 단위 테스트                       │
   │ - 코드 리뷰                         │
   │ - 결과물: 실행 가능한 코드          │
   └─────────────────────────────────────┘

4. 테스트 (Testing)
   ┌─────────────────────────────────────┐
   │ - 통합 테스트                       │
   │ - 시스템 테스트                     │
   │ - 인수 테스트                       │
   │ - 결과물: 테스트 보고서             │
   └─────────────────────────────────────┘

5. 유지보수 (Maintenance)
   ┌─────────────────────────────────────┐
   │ - 운영 배포                         │
   │ - 오류 수정                         │
   │ - 기능 개선                         │
   │ - 결과물: 업데이트된 시스템         │
   └─────────────────────────────────────┘
```

## 4. V-모델 (폭포수 변형)

```
폭포수 모델을 테스트 중심으로 확장한 것

        요구사항 분석 ─────────────── 인수 테스트
                 ↓                          ↑
             설계 ─────────────────── 시스템 테스트
                 ↓                          ↑
          상세 설계 ─────────────── 통합 테스트
                 ↓                          ↑
             구현 ──────────────── 단위 테스트
                 ↓                          ↑
                 └──────────────────────────┘
                           │
                    코딩 / 테스트

특징:
- 각 개발 단계에 대응하는 테스트 단계 존재
- 요구분석 → 인수 테스트 계획
- 설계 → 시스템 테스트 계획
- 상세 설계 → 통합 테스트 계획
- 구현 → 단위 테스트
```

## 5. 장단점

### 장점
```
1. 명확한 단계 구분
   - 각 단계가 명확히 구분됨
   - 진행 상황 파악 용이

2. 문서화 중심
   - 모든 단계에서 문서 산출물
   - 유지보수에 유리

3. 관리 용이
   - 진척도 관리 쉬움
   - 예측 가능한 일정

4. 요구사항 고정
   - 초기에 요구사항 확정
   - 범위 관리 용이
```

### 단점
```
1. 유연성 부족
   - 요구사항 변경 어려움
   - 이전 단계로 복귀 곤란

2. 실제 개발과 불일치
   - 실제로는 순차적으로 진행 안 됨
   - 변경 불가피

3. 사용자 피드백 지연
   - 개발 후반에야 결과 확인
   - 잘못된 방향이면 낭비 큼

4. 위험 노출 지연
   - 문제를 나중에 발견
   - 수정 비용 증가
```

## 6. 적용 분야

```
적합한 경우:
✓ 요구사항이 명확하고 변경이 적음
✓ 안전 중요 시스템 (원자력, 의료)
✓ 대규모 프로젝트
✓ 계약 기반 개발
✓ 경험이 적은 팀

부적합한 경우:
✗ 요구사항이 불명확
✗ 빠른 변경이 필요
✗ 혁신적인 제품 개발
✗ 스타트업, 초기 단계
```

## 7. 코드 예시

```python
from dataclasses import dataclass
from typing import List, Optional
from enum import Enum
from datetime import datetime

class Phase(Enum):
    """폭포수 모델 단계"""
    REQUIREMENTS = "요구사항 분석"
    DESIGN = "설계"
    IMPLEMENTATION = "구현"
    TESTING = "테스트"
    DEPLOYMENT = "배포"
    MAINTENANCE = "유지보수"

class PhaseStatus(Enum):
    """단계 상태"""
    PENDING = "대기"
    IN_PROGRESS = "진행중"
    COMPLETED = "완료"
    BLOCKED = "차단"

@dataclass
class Deliverable:
    """산출물"""
    name: str
    description: str
    status: str
    approved: bool = False

@dataclass
class WaterfallPhase:
    """폭포수 모델 단계 정보"""
    phase: Phase
    status: PhaseStatus
    start_date: Optional[datetime] = None
    end_date: Optional[datetime] = None
    deliverables: List[Deliverable] = None

    def __post_init__(self):
        if self.deliverables is None:
            self.deliverables = []

class WaterfallProject:
    """폭포수 모델 프로젝트 관리"""

    def __init__(self, name: str):
        self.name = name
        self.phases = self._initialize_phases()
        self.current_phase_idx = 0

    def _initialize_phases(self) -> List[WaterfallPhase]:
        """단계 초기화"""
        return [
            WaterfallPhase(Phase.REQUIREMENTS, PhaseStatus.PENDING),
            WaterfallPhase(Phase.DESIGN, PhaseStatus.PENDING),
            WaterfallPhase(Phase.IMPLEMENTATION, PhaseStatus.PENDING),
            WaterfallPhase(Phase.TESTING, PhaseStatus.PENDING),
            WaterfallPhase(Phase.DEPLOYMENT, PhaseStatus.PENDING),
            WaterfallPhase(Phase.MAINTENANCE, PhaseStatus.PENDING),
        ]

    def start_phase(self, phase: Phase) -> bool:
        """단계 시작"""
        phase_obj = self._get_phase(phase)
        if phase_obj is None:
            return False

        # 이전 단계 완료 확인
        phase_idx = self.phases.index(phase_obj)
        if phase_idx > 0:
            prev_phase = self.phases[phase_idx - 1]
            if prev_phase.status != PhaseStatus.COMPLETED:
                print(f"이전 단계({prev_phase.phase.value})가 완료되지 않았습니다.")
                return False

        phase_obj.status = PhaseStatus.IN_PROGRESS
        phase_obj.start_date = datetime.now()
        self.current_phase_idx = phase_idx
        return True

    def complete_phase(self, phase: Phase) -> bool:
        """단계 완료"""
        phase_obj = self._get_phase(phase)
        if phase_obj is None:
            return False

        # 모든 산출물 승인 확인
        unapproved = [d for d in phase_obj.deliverables if not d.approved]
        if unapproved:
            print(f"미승인 산출물: {[d.name for d in unapproved]}")
            return False

        phase_obj.status = PhaseStatus.COMPLETED
        phase_obj.end_date = datetime.now()
        return True

    def add_deliverable(self, phase: Phase, deliverable: Deliverable):
        """산출물 추가"""
        phase_obj = self._get_phase(phase)
        if phase_obj:
            phase_obj.deliverables.append(deliverable)

    def approve_deliverable(self, phase: Phase, deliverable_name: str) -> bool:
        """산출물 승인"""
        phase_obj = self._get_phase(phase)
        if phase_obj:
            for d in phase_obj.deliverables:
                if d.name == deliverable_name:
                    d.approved = True
                    return True
        return False

    def get_progress(self) -> dict:
        """진행 상황"""
        completed = sum(1 for p in self.phases if p.status == PhaseStatus.COMPLETED)
        return {
            "total_phases": len(self.phases),
            "completed_phases": completed,
            "current_phase": self.phases[self.current_phase_idx].phase.value,
            "progress_percent": (completed / len(self.phases)) * 100
        }

    def _get_phase(self, phase: Phase) -> Optional[WaterfallPhase]:
        """단계 객체 조회"""
        for p in self.phases:
            if p.phase == phase:
                return p
        return None

    def print_status(self):
        """상태 출력"""
        print(f"\n=== {self.name} 프로젝트 상태 ===\n")
        for p in self.phases:
            status_icon = {
                PhaseStatus.PENDING: "⬜",
                PhaseStatus.IN_PROGRESS: "🔵",
                PhaseStatus.COMPLETED: "✅",
                PhaseStatus.BLOCKED: "🔴"
            }.get(p.status, "?")
            print(f"{status_icon} {p.phase.value}: {p.status.value}")

            for d in p.deliverables:
                approved_icon = "✓" if d.approved else "○"
                print(f"   {approved_icon} {d.name}")


# 사용 예시
print("=== 폭포수 모델 시뮬레이션 ===\n")

# 프로젝트 생성
project = WaterfallProject("은행 시스템 구축")

# 요구사항 분석 단계
project.start_phase(Phase.REQUIREMENTS)
project.add_deliverable(Phase.REQUIREMENTS, Deliverable("SRS", "요구사항 명세서", "완료"))
project.add_deliverable(Phase.REQUIREMENTS, Deliverable("Usecase", "유스케이스 명세서", "완료"))

# 산출물 승인 및 단계 완료
project.approve_deliverable(Phase.REQUIREMENTS, "SRS")
project.approve_deliverable(Phase.REQUIREMENTS, "Usecase")
project.complete_phase(Phase.REQUIREMENTS)

# 설계 단계
project.start_phase(Phase.DESIGN)
project.add_deliverable(Phase.DESIGN, Deliverable("SDD", "설계 문서", "완료"))
project.add_deliverable(Phase.DESIGN, Deliverable("DB설계", "데이터베이스 설계서", "완료"))
project.approve_deliverable(Phase.DESIGN, "SDD")
project.approve_deliverable(Phase.DESIGN, "DB설계")
project.complete_phase(Phase.DESIGN)

# 구현 단계
project.start_phase(Phase.IMPLEMENTATION)
project.add_deliverable(Phase.IMPLEMENTATION, Deliverable("Source", "소스 코드", "진행중"))

# 상태 출력
project.print_status()

progress = project.get_progress()
print(f"\n전체 진행률: {progress['progress_percent']:.1f}%")
```

## 8. 폭포수 모델 vs 애자일

| 항목 | 폭포수 모델 | 애자일 |
|------|------------|--------|
| 접근 방식 | 순차적 | 반복적 |
| 요구사항 | 초기 고정 | 지속적 변경 |
| 문서화 | 강조 | 최소화 |
| 고객 참여 | 시작/끝 | 지속적 |
| 변경 대응 | 어려움 | 유연함 |
| 위험 노출 | 후반 | 초기 |

## 9. 실무에선? (기술사적 판단)
- **안전 중요 시스템**: 의료, 항공, 원자력 → 폭포수 권장
- **공공사업**: 입찰, 계약 기반 → 폭포수 적합
- **SI 프로젝트**: 명확한 요구사항 → 폭포수 기반
- **하이브리드**: 단계는 폭포수, 내부는 애자일
- **V-모델**: 테스트 중심 필요 시 변형 모델 사용

## 10. 관련 개념
- V-모델
- 나선형 모델
- 프로토타입 모델
- SDLC

---

## 어린이를 위한 종합 설명

**폭포수 모델은 "계단 내려가기" 같아요!**

### 어떻게 진행할까요? 🚶
```
1단계 요구사항: 뭘 만들까?
      ↓
2단계 설계: 어떻게 만들까?
      ↓
3단계 구현: 만들기!
      ↓
4단계 테스트: 잘 작동하나?
      ↓
5단계 배포: 사용하러 가요!
```

### 특징 💧
```
폭포처럼:
- 위에서 아래로만 흘러요
- 거꾸로 올라갈 수 없어요

장점:
- 순서가 명확해요 📋
- 계획이 세워요

단점:
- 중간에 바꾸기 어려워요 😓
- 처음에 잘 계획해야 해요
```

### 언제 쓸까요? 🤔
```
좋아요:
- 무엇을 만들지 정확히 알 때
- 나중에 바꾸지 않을 때

안 좋아요:
- 계속 바뀔 수 있을 때
- 새로운 것을 만들 때
```

**비밀**: 큰 건물을 지을 때 폭포수 모델처럼 계획적으로 해요! 🏢✨
