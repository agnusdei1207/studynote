---
title: "Support Vector Machine, SVM"
date: "2026-03-03"
tags:
  - "studynote-ai"
weight: 36
---
> **핵심 인사이트**
> 1. SVM (Support Vector Machine)은 두 클래스를 분리하는 초평면(Hyperplane) 중 마진(Margin, 클래스 간 간격)이 최대인 것을 찾는 최대 마진 분류기로, 소규모·고차원 데이터에서 강력한 성능을 보인다.
> 2. 커널 트릭(Kernel Trick)은 비선형 데이터를 고차원 특성 공간으로 암묵적으로 변환해 선형 분리를 가능하게 하며, 실제로 변환을 수행하지 않고 커널 함수만으로 내적을 계산한다.
> 3. 딥러닝 등장 이전 이미지·텍스트 분류의 표준이었으나, 대용량 데이터에서 확장성 한계가 있어 현재는 중소규모 고차원 데이터(NLP, 바이오인포매틱스)와 앙상블에 활용된다.

---

## I. 최대 마진 분류기

```
선형 분리 가능한 2클래스 문제:

     ●  ●             결정 경계 (hyperplane): w·x + b = 0
  ●           ○  ○
  ●    ------ / ------  <- 최대 마진 초평면
     ●  |    / | ○  ○
        |   /  |
  마진  | =   2/||w|| =   마진
  (좌)  |   /  |   (우)
        SV      SV
       (서포트 벡터)

마진 = 2/||w|| 을 최대화 하는 w, b 찾기
<=> ||w||^ 최소화 (이차 계획법, QP)
```

| 개념         | 설명                               |
|------------|-----------------------------------|
| 초평면       | w·x + b = 0, 클래스를 분리하는 경계 |
| 서포트 벡터  | 초평면과 가장 가까운 데이터 포인트   |
| 마진         | 서포트 벡터 간의 거리               |

> 📢 **섹션 요약 비유**: 두 나라 사이에 DMZ(비무장지대)를 최대한 넓게 만드는 것 — 경계가 애매하지 않을수록 분류가 안정적이다.

---

## II. 소프트 마진 SVM (Soft Margin)

```
선형 분리 불가능한 경우:
  슬랙 변수 ξi (xi >= 0) 도입
  -> 일부 오분류 허용

목적함수:
  min 0.5||w||^ + C * sum(ξi)

  C: 규제 파라미터
  C 크면: 오분류 최소화 (과적합 위험)
  C 작으면: 마진 최대화 (과소적합 위험)
```

| C값    | 마진     | 오분류 허용 | 효과           |
|-------|---------|---------|----------------|
| 크다   | 좁음    | 거의 없음 | 훈련 데이터 최적|
| 작다   | 넓음    | 많음     | 일반화 향상     |

> 📢 **섹션 요약 비유**: C가 크면 "틀리면 안 돼" 엄격한 선생님, C가 작으면 "약간 틀려도 돼" 너그러운 선생님 — 실제 데이터는 조금 유연한 게 낫다.

---

## III. 커널 트릭 (Kernel Trick)

```
1차원 데이터: XOR 문제 (선형 분리 불가)
  x=1: ●  x=2: ○  x=3: ●  x=4: ○

고차원 변환: φ(x) = (x, x^)
  (1,1): ●  (2,4): ○  (3,9): ●  (4,16): ○
  -> 2차원에서 선형 분리 가능!

커널 함수 K(xi, xj) = φ(xi)·φ(xj)
  내적을 직접 계산 (변환 불필요)

주요 커널:
  선형 커널:  K = xi·xj
  RBF 커널:   K = exp(-γ||xi-xj||^)  (가우시안)
  다항 커널:  K = (xi·xj + c)^d
```

> 📢 **섹션 요약 비유**: 구겨진 종이(2D 비선형)를 펼치면(고차원) 직선으로 나눌 수 있다 — 커널은 펼치는 비용 없이 결과만 계산한다.

---

## IV. SVM vs 딥러닝

| 비교       | SVM                  | 딥러닝 (CNN, Transformer)  |
|-----------|----------------------|--------------------------|
| 데이터 규모 | 소규모 강점           | 대규모 강점               |
| 차원       | 고차원 데이터 강점     | 자동 특성 추출            |
| 해석 가능성 | 중간 (서포트 벡터)    | 낮음 (블랙박스)           |
| 학습 시간  | 빠름 (소규모)         | 느림                      |
| 커널 튜닝  | 필요                  | 불필요 (자동)             |

> 📢 **섹션 요약 비유**: SVM은 정밀한 수술용 메스(소규모 고정밀), 딥러닝은 공장형 대량 생산 기계 — 상황에 맞게 선택.

---

## V. 실무 사용 영역

```
SVM 현재 활용:
1. 텍스트 분류 (고차원 희소 데이터): 스팸 필터, 감성 분석
2. 바이오인포매틱스: 유전자 발현 분류 (소규모 + 고차원)
3. 이상 탐지 (One-Class SVM): 정상 패턴 학습 후 이상 탐지
4. 앙상블 구성 요소: 다양성 확보를 위한 기저 분류기

Python sklearn:
  from sklearn.svm import SVC
  svm = SVC(C=1.0, kernel='rbf', gamma='scale')
  svm.fit(X_train, y_train)
```

> 📢 **섹션 요약 비유**: SVM은 데이터가 많지 않고 특성이 많은 전문 분야(바이오, 보안)에서 여전히 최강 — 딥러닝이 모든 걸 대체하지 않는다.

---

## 📌 관련 개념 맵

```
SVM (Support Vector Machine)
+-- 핵심: 최대 마진 초평면
|   +-- 서포트 벡터 (경계 데이터)
|   +-- 마진 = 2/||w||
+-- 확장
|   +-- 소프트 마진 (규제 C)
|   +-- 커널 트릭 (RBF, Poly, Linear)
+-- 멀티클래스
|   +-- OvR (One-vs-Rest)
|   +-- OvO (One-vs-One)
+-- 활용
    +-- 텍스트 분류 (고차원 희소)
    +-- 바이오인포매틱스
    +-- One-Class SVM (이상 탐지)
```

---

## 📈 관련 키워드 및 발전 흐름도

```
[Vapnik & Chervonenkis 이론 (1960s)]
통계적 학습 이론 (VC 이론)
      |
      v
[SVM 알고리즘 (Boser, Guyon, Vapnik, 1992)]
최대 마진 분류기 공식화
      |
      v
[커널 SVM (1995~)]
RBF 커널로 비선형 문제 해결
NLP·이미지 분류 표준
      |
      v
[딥러닝 등장 (2012~)]
ImageNet 이후 CNN이 이미지 분류 제패
SVM 사용 감소
      |
      v
[현재: 특수 영역 활용]
고차원 소규모 (바이오, 보안)
One-Class SVM (이상 탐지)
```

---

## 👶 어린이를 위한 3줄 비유 설명

1. SVM은 빨간 공과 파란 공을 나누는 선을 그을 때, 두 무리 사이에 가장 넓은 통로를 만드는 알고리즘이에요.
2. 직선으로 나눌 수 없을 때는 커널이라는 마법으로 공들을 더 높은 차원으로 올려 직선으로 나눠요.
3. 데이터가 많지 않고 특성이 복잡한 의학·보안 분야에서 지금도 자주 쓰인답니다!
