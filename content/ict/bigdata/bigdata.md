+++
title = "빅데이터 (Big Data)"
date = 2025-03-01

[extra]
categories = "ict-bigdata"
+++

# 빅데이터 (Big Data)

## 핵심 인사이트 (3줄 요약)
> **대량의 정형/비정형 데이터에서 가치를 창출**하는 기술. Volume(규모), Velocity(속도), Variety(다양성)의 3V 특성. 수집→저장→처리→분석→시각화의 파이프라인으로 활용.

## 1. 개념
빅데이터는 **기존 데이터베이스 관리 도구로 처리하기 어려운 대규모 데이터 집합**으로, 다양한 소스에서 생성되는 방대한 양의 정형/비정형 데이터를 의미한다.

> 비유: "데이터의 바다" - 엄청난 양의 데이터에서 진주(가치)를 찾는 것

## 2. 빅데이터의 특성 (3V → 5V)

```
┌─────────────────────────────────────────────────────────┐
│                 빅데이터의 특성 (5V)                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. Volume (규모)                                       │
│     - 페타바이트(PB), 엑사바이트(EB) 단위               │
│     - 예: 페이스북 일일 데이터 4PB                     │
│                                                         │
│  2. Velocity (속도)                                     │
│     - 실시간/근실시간 처리                              │
│     - 예: 주식 거래, 센서 데이터                       │
│                                                         │
│  3. Variety (다양성)                                    │
│     - 정형: DB, 스프레드시트                           │
│     - 반정형: JSON, XML, 로그                          │
│     - 비정형: 영상, 음성, 텍스트                       │
│                                                         │
│  4. Veracity (정확성)                                   │
│     - 데이터 품질, 신뢰성                              │
│     - 노이즈, 결측치 처리                              │
│                                                         │
│  5. Value (가치)                                        │
│     - 비즈니스 인사이트 도출                           │
│     - 의사결정 지원                                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 3. 빅데이터 처리 기술

### 3.1 하둡 (Hadoop)
```
하둡 에코시스템:

┌────────────────────────────────────────────────────────┐
│                    Hadoop Ecosystem                     │
├────────────────────────────────────────────────────────┤
│                                                        │
│   ┌─────────────┐    ┌─────────────┐                  │
│   │    HDFS     │    │ MapReduce   │                  │
│   │(분산 파일   │    │(분산 처리)  │                  │
│   │   시스템)   │    │             │                  │
│   └─────────────┘    └─────────────┘                  │
│                                                        │
│   ┌─────────────┐    ┌─────────────┐                  │
│   │    YARN     │    │   Hive      │                  │
│   │(자원 관리)  │    │(SQL 인터페이스)│                │
│   └─────────────┘    └─────────────┘                  │
│                                                        │
│   ┌─────────────┐    ┌─────────────┐                  │
│   │    HBase    │    │    Pig      │                  │
│   │(NoSQL DB)   │    │(스크립트)   │                  │
│   └─────────────┘    └─────────────┘                  │
│                                                        │
└────────────────────────────────────────────────────────┘

HDFS (Hadoop Distributed File System):
- 블록 단위 저장 (128MB)
- 복제 (기본 3본)
- 마스터-슬레이브 구조

MapReduce:
- Map: 데이터 분할, 변환
- Reduce: 집계, 결합
```

### 3.2 스파크 (Spark)
```
인메모리 분산 처리 프레임워크

특징:
- 하둡보다 100배 빠름 (인메모리)
- 배치 + 스트리밍 통합
- 다양한 API (SQL, ML, Graph)

구조:
┌─────────────────────────────────────┐
│           Spark Core                │
├─────────────────────────────────────┤
│  Spark SQL │ MLlib │ GraphX │ Streaming │
└─────────────────────────────────────┘
```

## 4. 빅데이터 분석 파이프라인

```
┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
│  수집    │──→│  저장    │──→│  처리    │──→│  분석    │──→│  시각화  │
└──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘

수집:
- 로그, 센서, SNS, IoT
- Flume, Kafka

저장:
- HDFS, S3, NoSQL
- Data Lake

처리:
- 정제, 변환, 통합
- ETL/ELT

분석:
- 통계, 마이닝, ML
- 통찰 도출

시각화:
- 대시보드, 리포트
- Tableau, Power BI
```

## 5. 코드 예시

```python
from dataclasses import dataclass
from typing import List, Dict, Any
from collections import Counter
import json

@dataclass
class DataPoint:
    """데이터 포인트"""
    timestamp: str
    source: str
    data: Dict[str, Any]

class DataCollector:
    """데이터 수집 시뮬레이션"""

    def __init__(self):
        self.collected_data: List[DataPoint] = []

    def collect(self, source: str, data: Dict) -> int:
        """데이터 수집"""
        import datetime
        point = DataPoint(
            timestamp=datetime.datetime.now().isoformat(),
            source=source,
            data=data
        )
        self.collected_data.append(point)
        return len(self.collected_data)

class DataProcessor:
    """데이터 처리"""

    def __init__(self):
        self.processed_data = []

    def clean(self, data: List[DataPoint]) -> List[DataPoint]:
        """데이터 정제"""
        cleaned = []
        for point in data:
            # 결측치 제거
            if point.data is not None:
                cleaned.append(point)
        return cleaned

    def transform(self, data: List[DataPoint]) -> List[Dict]:
        """데이터 변환"""
        transformed = []
        for point in data:
            item = {
                'timestamp': point.timestamp,
                'source': point.source,
                **point.data
            }
            transformed.append(item)
        return transformed

class MapReduceSimulator:
    """MapReduce 시뮬레이션"""

    @staticmethod
    def map_phase(data: List[Dict], key_field: str) -> Dict[str, List]:
        """Map 단계: 키별 그룹화"""
        mapped = {}
        for item in data:
            key = item.get(key_field, 'unknown')
            if key not in mapped:
                mapped[key] = []
            mapped[key].append(item)
        return mapped

    @staticmethod
    def reduce_phase(mapped: Dict[str, List],
                     reduce_func) -> Dict[str, Any]:
        """Reduce 단계: 집계"""
        reduced = {}
        for key, values in mapped.items():
            reduced[key] = reduce_func(values)
        return reduced

# 사용 예시
print("=== 빅데이터 처리 시뮬레이션 ===\n")

# 수집
collector = DataCollector()
collector.collect("web_log", {"page": "/home", "user_id": "user1", "duration": 30})
collector.collect("web_log", {"page": "/product", "user_id": "user2", "duration": 45})
collector.collect("web_log", {"page": "/home", "user_id": "user3", "duration": 20})
collector.collect("sensor", {"device": "temp_01", "value": 25.5})
collector.collect("sensor", {"device": "temp_01", "value": 26.0})

print(f"수집된 데이터: {len(collector.collected_data)}건")

# 처리
processor = DataProcessor()
cleaned = processor.clean(collector.collected_data)
transformed = processor.transform(cleaned)

# MapReduce: 페이지별 방문 횟수
web_data = [t for t in transformed if t['source'] == 'web_log']
mapped = MapReduceSimulator.map_phase(web_data, 'page')
page_counts = MapReduceSimulator.reduce_phase(
    mapped,
    lambda values: len(values)
)

print("\n페이지별 방문 횟수:")
for page, count in page_counts.items():
    print(f"  {page}: {count}회")

# 센서 데이터 평균
sensor_data = [t for t in transformed if t['source'] == 'sensor']
mapped = MapReduceSimulator.map_phase(sensor_data, 'device')
avg_values = MapReduceSimulator.reduce_phase(
    mapped,
    lambda values: sum(v['value'] for v in values) / len(values)
)

print("\n센서별 평균값:")
for device, avg in avg_values.items():
    print(f"  {device}: {avg:.2f}")
```

## 6. 빅데이터 활용 분야

```
1. 비즈니스
   - 고객 행동 분석
   - 수요 예측
   - 마케팅 최적화

2. 공공
   - 스마트시티
   - 교통 분석
   - 재난 예측

3. 의료
   - 질병 예측
   - 신약 개발
   - 진단 보조

4. 금융
   - 사기 탐지
   - 신용 평가
   - 알고리즘 트레이딩
```

## 7. 장단점

### 장점
| 장점 | 설명 |
|-----|------|
| 통찰력 | 숨겨진 패턴 발견 |
| 의사결정 | 데이터 기반 판단 |
| 효율성 | 프로세스 최적화 |
| 혁신 | 새로운 비즈니스 |

### 단점
| 단점 | 설명 |
|-----|------|
| 비용 | 인프라, 인력 |
| 복잡성 | 기술적 난이도 |
| 프라이버시 | 개인정보 이슈 |
| 품질 | 데이터 품질 관리 |

## 8. 실무에선? (기술사적 판단)
- **플랫폼**: AWS EMR, GCP BigQuery, Azure Synapse
- **실시간**: Kafka + Spark Streaming
- **Data Lake**: S3 + Glue + Athena
- **ML 파이프라인**: MLflow, Kubeflow

## 9. 관련 개념
- 데이터 웨어하우스
- 데이터 마이닝
- 머신러닝
- 하둡

---

## 어린이를 위한 종합 설명

**빅데이터는 "엄청 큰 정보 창고"예요!**

### 얼마나 클까요? 📊
```
일반 데이터:
"책상 서랍 정도"

빅데이터:
"도서관 전체!"
- 페이스북 하루에 4PB
  (사진 4000억 장)
```

### 3가지 특성 🔢
```
Volume: 엄청 많아요!
Velocity: 빨리 들어와요!
Variety: 여러 가지 형태예요!
  - 글, 사진, 영상, 소리
```

### 무엇을 할까요? 🎯
```
유튜브:
"어떤 영상을 좋아할까?"

넷플릭스:
"다음엔 뭘 볼까?"

아마존:
"이거도 살래?"
```

**비밀**: 내가 본 것, 클릭한 것 다 기록돼요! 📱✨
