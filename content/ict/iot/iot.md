+++
title = "사물인터넷 (IoT)"
date = 2025-03-01

[extra]
categories = "ict-iot"
+++

# 사물인터넷 (IoT, Internet of Things)

## 핵심 인사이트 (3줄 요약)
> **사물에 센서와 통신 기능을 탑재해 인터넷으로 연결**하는 기술. 데이터 수집→전송→분석→제어의 흐름으로 스마트 환경 구현. 스마트홈, 스마트시티, 산업 IoT 등 광범위하게 활용.

## 1. 개념
사물인터넷(IoT)은 **다양한 사물에 센서, 통신, 처리 기능을 내장하여 인터넷으로 연결**하고, 수집된 데이터를 분석해 지능형 서비스를 제공하는 기술이다.

> 비유: "사물이 말하는 세상" - 냉장고가 우유 떨어진 걸 알려주고, 차가 정비 시기를 알려줌

## 2. IoT 구조 (4계층)

```
┌────────────────────────────────────────────────────────┐
│                    IoT 4계층 구조                       │
├────────────────────────────────────────────────────────┤
│                                                        │
│  4계층: 응용 계층 (Application Layer)                  │
│  ┌────────────────────────────────────────────────┐   │
│  │ 스마트홈, 스마트시티, 스마트팜, 스마트팩토리    │   │
│  │ 헬스케어, 커넥티드카                           │   │
│  └────────────────────────────────────────────────┘   │
│                       ↑                               │
│  3계층: 처리 계층 (Processing Layer)                  │
│  ┌────────────────────────────────────────────────┐   │
│  │ 클라우드 플랫폼, 빅데이터 분석                  │   │
│  │ AI/ML, 데이터 저장 및 처리                      │   │
│  └────────────────────────────────────────────────┘   │
│                       ↑                               │
│  2계층: 네트워크 계층 (Network Layer)                 │
│  ┌────────────────────────────────────────────────┐   │
│  │ 게이트웨이, 5G/LTE, WiFi, 블루투스             │   │
│  │ LoRa, Zigbee, NB-IoT                           │   │
│  └────────────────────────────────────────────────┘   │
│                       ↑                               │
│  1계층: 디바이스 계층 (Device Layer)                  │
│  ┌────────────────────────────────────────────────┐   │
│  │ 센서 (온도, 습도, 조도, 가속도, GPS)            │   │
│  │ 액추에이터 (모터, 릴레이)                       │   │
│  │ 임베디드 시스템 (MCU)                           │   │
│  └────────────────────────────────────────────────┘   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

## 3. IoT 통신 프로토콜

```
┌────────────────────────────────────────────────────────┐
│                  IoT 통신 프로토콜                      │
├────────────────────────────────────────────────────────┤
│                                                        │
│  단거리 (Short Range):                                 │
│  - Bluetooth LE: 저전력, 개인용 기기                  │
│  - Zigbee: 메시 네트워크, 스마트홈                    │
│  - Z-Wave: 홈 오토메이션                              │
│  - NFC: 근거리 결제, 인증                             │
│                                                        │
│  중거리 (Medium Range):                                │
│  - WiFi: 고속 데이터 전송                             │
│  - Thread: IPv6 기반, 저전력                          │
│                                                        │
│  장거리 (Long Range - LPWAN):                          │
│  - LoRa: 장거리, 저전력, 저속                         │
│  - NB-IoT: 셀룰러 기반, 안정적                        │
│  - Sigfox: 초저전력, 소량 데이터                      │
│                                                        │
│  메시지 프로토콜:                                      │
│  - MQTT: 발행-구독, 경량                              │
│  - CoAP: 제약 환경용 HTTP                             │
│  - AMQP: 신뢰성 있는 메시징                           │
│                                                        │
└────────────────────────────────────────────────────────┘
```

## 4. 코드 예시

```python
from dataclasses import dataclass
from typing import Dict, List, Optional, Callable
from datetime import datetime
from enum import Enum
import json
import random

class SensorType(Enum):
    TEMPERATURE = "온도"
    HUMIDITY = "습도"
    MOTION = "동작"
    LIGHT = "조도"
    GPS = "위치"

@dataclass
class SensorData:
    """센서 데이터"""
    device_id: str
    sensor_type: SensorType
    value: float
    unit: str
    timestamp: datetime

    def to_json(self) -> str:
        return json.dumps({
            'device_id': self.device_id,
            'sensor_type': self.sensor_type.value,
            'value': self.value,
            'unit': self.unit,
            'timestamp': self.timestamp.isoformat()
        })

class IoTSensor:
    """IoT 센서 시뮬레이션"""

    def __init__(self, device_id: str, sensor_type: SensorType, unit: str):
        self.device_id = device_id
        self.sensor_type = sensor_type
        self.unit = unit
        self.callback: Optional[Callable] = None

    def read(self) -> SensorData:
        """센서 값 읽기"""
        # 시뮬레이션용 랜덤 값
        if self.sensor_type == SensorType.TEMPERATURE:
            value = round(random.uniform(18.0, 28.0), 1)
        elif self.sensor_type == SensorType.HUMIDITY:
            value = round(random.uniform(30.0, 70.0), 1)
        elif self.sensor_type == SensorType.LIGHT:
            value = round(random.uniform(0, 1000), 0)
        else:
            value = random.randint(0, 1)

        data = SensorData(
            device_id=self.device_id,
            sensor_type=self.sensor_type,
            value=value,
            unit=self.unit,
            timestamp=datetime.now()
        )

        if self.callback:
            self.callback(data)

        return data

class IoTHub:
    """IoT 허브 (게이트웨이)"""

    def __init__(self, hub_id: str):
        self.hub_id = hub_id
        self.devices: Dict[str, IoTSensor] = {}
        self.data_buffer: List[SensorData] = []
        self.rules: List[Dict] = []

    def register_device(self, device: IoTSensor):
        """디바이스 등록"""
        device.callback = self._on_data_received
        self.devices[device.device_id] = device
        print(f"[{self.hub_id}] 디바이스 등록: {device.device_id}")

    def _on_data_received(self, data: SensorData):
        """데이터 수신 콜백"""
        self.data_buffer.append(data)
        self._check_rules(data)

    def add_rule(self, device_id: str, condition: Callable, action: Callable):
        """자동화 규칙 추가"""
        self.rules.append({
            'device_id': device_id,
            'condition': condition,
            'action': action
        })

    def _check_rules(self, data: SensorData):
        """규칙 확인 및 실행"""
        for rule in self.rules:
            if rule['device_id'] == data.device_id:
                if rule['condition'](data.value):
                    rule['action'](data)

    def collect_all(self) -> List[SensorData]:
        """모든 센서 데이터 수집"""
        return [device.read() for device in self.devices.values()]

    def get_statistics(self) -> Dict:
        """데이터 통계"""
        if not self.data_buffer:
            return {}

        stats = {}
        for data in self.data_buffer:
            key = data.sensor_type.value
            if key not in stats:
                stats[key] = {'values': [], 'count': 0}
            stats[key]['values'].append(data.value)
            stats[key]['count'] += 1

        for key in stats:
            values = stats[key]['values']
            stats[key]['avg'] = sum(values) / len(values)
            stats[key]['min'] = min(values)
            stats[key]['max'] = max(values)

        return stats


# 사용 예시
print("=== IoT 시스템 시뮬레이션 ===\n")

# 허브 생성
hub = IoTHub("SMART_HOME_01")

# 센서 등록
temp_sensor = IoTSensor("TEMP_001", SensorType.TEMPERATURE, "°C")
humidity_sensor = IoTSensor("HUM_001", SensorType.HUMIDITY, "%")
light_sensor = IoTSensor("LIGHT_001", SensorType.LIGHT, "lux")

hub.register_device(temp_sensor)
hub.register_device(humidity_sensor)
hub.register_device(light_sensor)

# 자동화 규칙
def turn_on_ac(data):
    print(f"  → 에어컨 켜기! (온도: {data.value}°C)")

def turn_on_lights(data):
    print(f"  → 조명 켜기! (조도: {data.value}lux)")

hub.add_rule("TEMP_001", lambda v: v > 26, turn_on_ac)
hub.add_rule("LIGHT_001", lambda v: v < 100, turn_on_lights)

# 데이터 수집
print("\n--- 센서 데이터 수집 ---")
for _ in range(3):
    hub.collect_all()

# 통계
print("\n--- 센서 데이터 통계 ---")
stats = hub.get_statistics()
for sensor_type, data in stats.items():
    print(f"{sensor_type}: 평균 {data['avg']:.1f}, "
          f"최소 {data['min']:.1f}, 최대 {data['max']:.1f}")
```

## 5. IoT 플랫폼

```
글로벌 IoT 플랫폼:
- AWS IoT Core
- Microsoft Azure IoT Hub
- Google Cloud IoT
- IBM Watson IoT

국내 IoT 플랫폼:
- KT GiGA IoT
- SKT IoT Hub
- LG U+ IoT Platform
- 네이버 클라우드 IoT
```

## 6. IoT 보안 이슈

```
주요 보안 위협:
1. 디바이스 하이재킹
   - 취약한 기본 비밀번호
   - 펌웨어 변조

2. 데이터 유출
   - 통신 구간 도청
   - 클라우드 침해

3. 서비스 거부 (DDoS)
   - Mirai 봇넷
   - 디바이스 악용

4. 프라이버시 침해
   - 위치 추적
   - 생활 패턴 수집

보안 대책:
- 디바이스 인증
- 통신 암호화 (TLS)
- 정기 펌웨어 업데이트
- 네트워크 분리
```

## 7. 장단점

### 장점
| 장점 | 설명 |
|-----|------|
| 자동화 | 스마트 제어 |
| 효율성 | 리소스 최적화 |
| 모니터링 | 실시간 상태 파악 |
| 편의성 | 원격 제어 |

### 단점
| 단점 | 설명 |
|-----|------|
| 보안 | 해킹 위험 |
| 프라이버시 | 개인정보 수집 |
| 복잡성 | 시스템 복잡 |
| 비용 | 구축 비용 |

## 8. 실무에선? (기술사적 판단)
- **스마트홈**: MQTT + 클라우드 플랫폼
- **스마트팩토리**: 산업용 게이트웨이 + Edge 컴퓨팅
- **스마트시티**: NB-IoT/LoRa + 대규모 플랫폼
- **보안**: Zero Trust, 디바이스 인증 필수

## 9. 관련 개념
- 엣지 컴퓨팅
- 5G
- 센서 네트워크
- 디지털 트윈

---

## 어린이를 위한 종합 설명

**IoT는 "사물이 인터넷으로 대화해요!"**

### 어떻게 작동할까요? 📱
```
냉장고 → "우유 없어요!"
스마트폰 → "알겠어요, 주문할게요!"
온라인몰 → "우유 배송 시작!"
```

### 어디에 쓰나요? 🏠
```
집: 불 켜기, 난방, CCTV
학교: 출석, 온도 조절
도시: 교통신호, 주차
공장: 기계 상태 확인
```

### 센서가 뭔가요? 👀
```
온도 센서: "더워요/추워요"
동작 센서: "누가 왔어요!"
빛 센서: "어두워요/밝아요"
```

**비밀**: 토스트기도 인터넷에 연결할 수 있어요! 🍞✨
